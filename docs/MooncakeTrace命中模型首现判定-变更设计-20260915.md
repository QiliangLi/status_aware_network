# Mooncake trace 命中模型变更设计:从冻结目录改为全局首现判定

| 项目 | 约定 |
|---|---|
| 文档版本 | 1.2,2026-09-15(依用户裁定:**取消训练/评价切分与 B\* 冻结,参数全部在线化或取零标定默认,全量 trace 直接打分,结果完整展示全部 baseline**;v1.1 的 D3=尾块参与维持) |
| 性质 | **变更设计文档(实施指导)**,尚未实现;本文档获用户确认后进入实施 |
| 修改对象 | 规格 §5.5 的 `frozen_history_catalog` 导入合同 |
| 动机 | 用户指定:同一 hash id 第一次出现视为未命中、第二次及之后出现视为命中;按此规则对 trace 全局预扫一遍,把每条请求的命中长度写成一个新字段,仿真框架直接读取 |
| 上游 | [规格 §5.5](共享KV统一队列Batch与错峰调度-仿真实验设计实施与测试规格-20260915.md)(现行合同)、[大白话解读 §3](../大白话解读-共享KV组批错峰仿真实施-20260915.md) |
| 实施纪律 | 按 AGENTS.md §2:本变更为口径修正,须在新文档显式记录"改了什么、为什么";新旧口径的实验结果**不得混用** |

---

## 1. 语义变更:改了什么、为什么

### 1.1 现行口径(frozen_history_catalog,规格 §5.5)

- 取每份 trace 时间最早的 **20% 请求**,把它们的前 512-token 完整块插入一棵**前缀 trie**,构成"缓存目录";
- 之后(训练/评价段)每条请求从首块沿自己的 hash 序列在 trie 上走,**走到第一个不在树上的块停止**,得连续命中块数 k;
- h = min(512·k, input_length−1),u = input_length−h;
- 目录**运行期间不增不减**,三个文件目录互相隔离。

设计意图:命中机会对所有策略完全相同,防止"完成快的策略动态制造命中"把缓存机制混进调度收益。

### 1.2 新口径(first-seen,全局首现判定)

- 按 (timestamp, source_line) 稳定顺序**全局扫一遍** trace,维护一个"已见 hash 集合";
- 每条请求:某块 hash **在它之前的任何请求中出现过** → 该块视为命中;**全局第一次出现** → 未命中;
- 扫完一条请求后,才把它自己的完整块加入"已见集合"(同一条请求内部的重复块**不互相可见**);
- 命中长度 h 与 u 同样按"至少留 1 个 token 现场计算"保护:h = min(命中 token 数, input_length−1);
- 每条请求的 h **一次性预计算**,作为新字段写入增强 trace 文件;仿真导入时直接读字段,不再做 trie 查询。

两者本质区别:**目录的构成与增长方式**。旧口径 = "只有最早的 20% 请求贡献缓存,之后冻结";新口径 = "全部历史请求按时间顺序滚动贡献缓存,越晚的请求可命中的越多"。新口径更接近真实系统里"用过的前缀都会被缓存"的动态行为;它对调度对比仍然公平——因为命中长度是**预先离线算好、写死在 trace 字段里**的,与任何策略的运行行为无关。

## 2. 决策记录(用户裁定)

| # | 决策点 | 裁定 | 说明 |
|---|---|---|---|
| D1 | 命中是否要求从头连续 | **连续前缀** | prefill 的 KV cache 是前缀性的,稀疏命中物理不可用 |
| D2 | 同一请求内重复块互相可见性 | **不可见**(整条请求处理完才入集合) | 位置编码决定同内容块不能跨位置复用 |
| D3 | 尾块(不满 512)参与判定 | **参与**(用户裁定) | hash_id 为内容指纹,经三份 trace 全量检验自洽(角色冲突=0、长度冲突=0);完全按请求显示的 hash_ids 判定 |
| D4 | 数据切分 | **取消训练/评价切分,全量打分**(用户裁定) | 参数不做预先标定(见 2.1),无需训练段;窗口仅作为运行与统计分片,**全部窗口参与打分**。最早 20% 冷启动期照常打分,报告同时给出"剔除冷启动段"的对照口径 |
| D5 | 命中口径 | **仅 first_seen,删除 frozen**(用户裁定 v1.4) | 不做双模式开关;frozen 导入路径(PrefixCatalog/trie 查询/建目录切分)与其专属测试一并删除,由 first_seen 实现与 T14–T22 取代;frozen 口径的历史定义与验收指纹保留在规格 §5.5,不随代码保留 |
| D6 | 参照与呈现 | **取消 B\*,完整展示全部 baseline**(用户裁定) | 每个策略的完整指标全部呈现;预注册判据改为相对 FCFS(机制投影,无事后挑选);E24 锚点定位由"B\* 满足率 20–95%"改为"FCFS 满足率 20–95%" |

### 2.1 为什么参数可以不预先冻结(用户质疑的正面回答)

"作弊"风险不在"参数随数据变",而在**研究者看了打分结果之后回头改配置**。在线公式是先验写死的,与 gate=0 同样在任何打分前定型,方法论上干净。逐参数的处置:

| 原调参项 | 原设计 | 新处置 | 是否还需要数据 |
|---|---|---|---|
| B\* | 训练段跑 7 个简单基线选最强并冻结 | **取消**(D6):全 baseline 完整展示,判据相对 FCFS | 否 |
| gate(MPC 门槛) | 训练段标定预测误差 P95 | **固定 gate=0**(规格默认值,零标定) | 否 |
| W_guard(老化阈值) | 2×train_P95(T0) | **在线滚动**:2×已到达请求 T0 的滚动 P95(请求到达即知自身 T0,无未来信息) | 否(运行时自算) |
| r_ref(配对参考) | 训练集几何均值 | **在线滚动几何均值**(已到达请求,无未来信息) | 否(运行时自算) |

附带说明:首现模型的全局预扫把"该请求之前的全部历史"编码进 hit 字段(含最早流量对已见集合的贡献),因此跨窗口的缓存状态传递是静态完成的——窗口切分不再承载任何训练/评价职责,仅用于运行分片与块 bootstrap 统计。

## 3. 精确定义与预处理算法

### 3.1 伪代码(方案 A/A/A)

```text
seen = 空集合                       # 已见 hash(来自更早 timestamp 组的全部块,含尾块)
rows 按 (timestamp, source_line) 升序
i = 0
while i < len(rows):
    j = 下一个 timestamp 不同处的行号     # [i, j) = 同一毫秒并发到达的请求组(D7)
    for row in rows[i:j]:                # 组内全部请求的视图 = 组前集合
        n_full = floor(row.input_length / 512)
        k = 0
        for x in 0 .. len(hash_ids)-1:   # 完整块与尾块都参与判定(D3=参与)
            if row.hash_ids[x] in seen: k += 1
            else: break                  # 连续前缀:首个未命中即停(D1=连续)
        hit = 512*k
        if k > n_full:                   # 尾块也命中 → 全命中,留 1 token
            hit = row.input_length - 1
        row.hit_tokens = min(hit, row.input_length - 1)
        row.u_tokens   = row.input_length - row.hit_tokens
        row.full_hit_adjusted = (hit >= row.input_length - 1)
    for row in rows[i:j]:                # 组处理完后统一入集合(请求内与组内均不可见)
        seen ∪= row.hash_ids
    i = j
输出:增强 trace(每行新增 3 个字段)
```

### 3.2 产物文件与格式

- **原始三份文件一律不改**(它们是官方发布、SHA256 验收的输入);
- 新增派生文件:`mooncake_trace/derived/<原名>.hit.jsonl`,每行 = 原行全部字段 + `"hit_tokens": int, "u_tokens": int, "full_hit_adjusted": bool`;
- 同目录生成 `derived/derive_report.json`:输入文件 SHA256、行数、输出 SHA256、按段统计(弃置/训练/评价各自的请求命中比例、token 命中比例、h/u 分位数)——作为**新口径的验收指纹**;
- `derived/` 不入库(mooncake_trace 整体不入库的既定纪律),由预处理脚本可重现生成;`derive_report.json` 的关键数字抄录进本文档附录与 manifest。

### 3.3 新口径验收指纹(实测,2026-09-15;全量 trace,与窗口切分无关)

按方案 A/A/A 对三份 trace 全量扫描的结果(与旧口径对照,详见第 6 节):

| 文件 | token 命中比例(新) | 请求 h>0 比例(新,弃置段之后) | u 中位数(旧→新) | 并发结构(时刻数/最大同刻组) |
|---|---:|---:|---:|---|
| conversation | **0.374** | 1.000 | 5408 → **2010** | 1,180 / 28 |
| toolagent | **0.570** | 1.000 | 861 → **681** | 1,180 / 47 |
| synthetic | **0.651** | 0.531 | 2458 → **348** | 3,982 / 2 |

(以上为 D7 同刻不可见口径。两项数据检验:① 三份 trace 各 18.3 万/18.3 万/4.4 万个不同 hash 中,"同一 hash 既以完整块又以尾块角色出现"= 0、"同一 hash 以不同长度尾块出现"= 0,hash 为内容指纹的假设自洽;② conversation/toolagent 的并发结构极端——同刻到达占 100%,平均每毫秒 10–20 条;synthetic 几乎逐条错开。两份真实 trace 与官方合成 trace 的并发形态完全不同,流量结构差异本身是重要实验背景。)

实现完成后,预处理脚本输出必须与上表逐位一致(差异仅允许来自决策点组合的不同)。

## 4. 代码修改清单

### 4.1 新增

| 文件 | 内容 |
|---|---|
| `tools/cq_derive_hits.py` | 预处理入口:读原始三份 → 3.1 算法(单一实现,D1/D2/D3/D7 已定案,无模式开关)→ 写 `derived/*.hit.jsonl` + `derive_report.json`;`--check-only` 只校验已有派生文件与报告一致 |
| `tests/test_cq_derive.py` | 新口径单元测试(见 5.1) |

### 4.2 修改

| 文件 | 改动 | 说明 |
|---|---|---|
| `sim/cq/trace.py` | **重写导入为 first_seen 单模式**:`import_mooncake` 读 `derived/<名>.hit.jsonl`,`h_u_of` 直接返回行内 hit/u 字段并校验 h+u=input、u≥1;**删除** PrefixCatalog、trie 查询、build_end/train_end 建目录切分与 `_hu`;`TraceImport` 字段按新口径定义(全量统计 + 派生文件 SHA256) | 单模式,无开关 |
| `sim/experiments/cq_common.py` | `MooncakeSource(fname, trace_dir, cache_model="first_seen")`:构造时按模式分流;`λ0` 用全量重算;**train_blocks/eval_blocks 合并为统一窗口列表(全部打分)**;W_guard/r_ref 改在线滚动 | D5 默认 first_seen |
| `sim/experiments/e19_profile.py` | `import_report` 输出双模式统计并列对照;新指纹取自 `derive_report.json` | E19 成为两种口径的对照面板 |
| `sim/experiments/e21/e22/e23/e24` | 无直接改动(全部经 `MooncakeSource`);正式重跑时结果目录加 `cache_model` 段以隔离 | 旧结果不覆盖 |
| `configs/cq/frozen-e19-e24.json` | manifest 增加 `cache_catalog` 版本与新旧指纹;**删除 B\* 冻结对象,登记 gate=0 与在线参数公式** | 配置显式记录口径版本 |

D5 已裁定:first_seen 为默认,frozen 显式传参可跑,结果目录按口径隔离。

### 4.3 不需要改

`engine/storage/policies/search/exact/metrics/simrun/observable` 全部不动——命中模型只影响 trace 导入层产出的 h/u;内核只消费 `RequestSpec`。这也是把预计算放进 trace 字段的直接好处:**仿真框架零感知**。

## 5. 测试修改清单

### 5.1 新增测试(test_cq_derive.py,编号接续 T14)

| ID | 夹具 | 断言 |
|---|---|---|
| T14 首现未命中 | 目录空,请求 [A,B] | hit=0(u=input) |
| T15 二次命中 | 请求1 [A,B]、请求2 [A,C] | 请求1 hit=0;请求2 连续命中 A → hit=512 |
| T16 非连续不算 | 请求1 [A,B]、请求2 [C,A] | 请求2 首块 C 未命中即停 → hit=0(**不因 A 出现过而记命中**) |
| T17 请求内不可见 | 单请求 [A,A] | hit=0(D2=A) |
| T18 尾块不参与 | input=700、[A,B],前请求已有 A | 只 1 个完整块 → hit=512、u=188(D3=A) |
| T19 全命中留一口 | input=1024、[A,B] 均已见 | hit=1023、u=1、full_hit_adjusted=true |
| T20 派生文件一致 | 三份真实 trace | `tools/cq_derive_hits.py --check-only` 通过;统计与 §3.3 指纹逐位一致 |
| T21 双模式共存 | 同文件两模式导入 | frozen 结果与现状回归一致;first_seen 的 h/u 恒满足 h+u=input、u≥1 |
| T22 同刻不可见 | 同 timestamp 三条 [A,B] / [A,C] / [A,D] | 第一条 hit=0;同刻的第二、三条也全部 hit=0(互相不可见);下一个 timestamp 的 [A,E] 命中 A |

### 5.2 存量测试处置(D5=v1.4 后)

与 frozen 绑定的测试**删除**:`test_cq_trace.py` 的 T02(trie 连续前缀)/T03(尾块)/T04(全命中)/T05(冷数据)与 `test_e19_import_fingerprints`——其语义已由 T14–T22 在 derive 工具上重新覆盖。与口径无关的测试保留:T01(文件指纹)、T06(同 timestamp 稳定排序)、T07(缩放)、T08(分块)、T09(CRN 守恒,改经新导入)、T10(不丢长请求)、T11–T13(统计口径)。全量 `pytest tests/ -q` 须全绿(预计 115 − 5 + 9 ≈ 119 项,以实际为准)。

## 6. 研究影响预警(必须重跑,新旧不可混)

用第 3.1 节算法实测三份 trace(弃置段之后),关键量变化:

| 量 | 变化 | 连锁影响 |
|---|---|---|
| token 命中比例 | 12.8%→37.3% / 40.8%→57.0% / 28.9%→65.0% | 读取量 V=κ·h·32 整体上升,共享带宽压力**加大** |
| u 中位数 | 5408→2010 / 861→681 / 2458→373 | 短 u 请求占比上升 |
| u 均值 | 仍为数千(p95 一至三万) | 批内 N=Σu 大概率仍 > N_sat=512,**E21 的 G≈1 结论方向上可能保持**,但必须重跑验证——短 u 批的出现可能让部分格点 G>1 |
| λ0 | 重算(变小:V↑) | 所有 ρ 格点的绝对负载变化,主矩阵须整体重跑 |
| T0/SLO | T0 = 读取↑ + 计算↓ 的净效应,deadline 全体移动 | E21/E23 的满足率绝对值与旧口径不可比 |

结论:**E19–E24 的全部冒烟数字在新口径下作废并重跑**;正式报告只引用与所声明口径一致的数据。两套口径允许并存为对照面板(frozen = "静态目录"下界情景,first_seen = "全历史滚动缓存"情景),这本身是一个有价值的敏感性维度。

D4/D6 的连锁调整:① E23 主矩阵不再区分训练/评价窗口,全部 15+5 个窗口参与打分,报告按"全量"与"剔除最早 20% 冷启动段"两个口径各出一版对照;② 预注册 headline 判据由"相对冻结 B\* ≥5pp 且 CI 下界>0"改为**相对 FCFS** 的同门槛,其余基线完整展示不参与单一判据;③ E24 锚点定位改用 FCFS 满足率 20–95%;④ 报告呈现层:每格点输出全部策略的完整指标表(SLO/TTFT/P95/STALL/回退率等),不再只给"vs B\*"一列。

## 7. 文档修改清单

| 文档 | 修改 | 时机 |
|---|---|---|
| 规格 §5.5 | **不修改原文**;规格头部"输入依据"增加本文档链接,并在 §5.5 首行加一行勘误注记:"2026-09-15 起命中模型可按 [变更设计] 切换 first-seen,frozen 为对照口径" | 实施合入时 |
| 《测试报告》 | 元信息表加 cache_model/gate/在线参数行;新增"命中模型变更与重跑结果"一节;**全部表格改为完整 baseline 呈现(不再单列 B\*)** | 重跑完成后 |
| 《大白话解读》§3.2/3.3 | 改写为双口径讲解(冻结目录 vs 全局首现),保留决策点说明 | 实施合入时 |
| `configs/cq/frozen-e19-e24.json` | `cache_catalog` 对象记录双口径指纹 | 实施合入时 |

## 8. 实施顺序与验收标准

```text
第 1 步  tools/cq_derive_hits.py + tests/test_cq_derive.py(T14–T22)
         验收:派生文件生成,--check-only 过,§3.3 指纹逐位一致,全量 pytest 绿
第 2 步  sim/cq/trace.py 重写为 first_seen 单模式(删 frozen)+ MooncakeSource 统一窗口/全量 λ0 + W_guard/r_ref 在线化 + gate=0
         验收:删除 frozen 专属测试后全量 pytest 绿;导入校验 h+u=input、u≥1
第 3 步  E19 双模式对照面板 + manifest 口径登记
         验收:derive_report 数字进入 manifest;E19 冒烟图含双口径对照
第 4 步  E21/E23/E24 冒烟重跑(first_seen 口径),结果目录按口径隔离
         验收:与旧口径对照表写入测试报告新增节;负收益格点照常保留
第 5 步  文档更新(§7 清单)与分逻辑提交推送
```

每步退出条件均为"该步新增测试全绿 + 存量 115 项不回归";禁止跳步合并提交。

---

## 附录:新旧口径全量统计对照(实测)

| 文件 | 段 | 旧 token 命中 | 新 token 命中 | 旧 u 中位 | 新 u 中位 | 新 u p95 | 新 u 均值 |
|---|---|---:|---:|---:|---:|---:|---:|
| conversation | 弃置段之后 | 0.128 | 0.374 | 5408 | 2010 | 28274 | 7074 |
| toolagent | 弃置段之后 | 0.408 | 0.571 | 861 | 681 | 16871 | 3495 |
| synthetic | 弃置段之后 | 0.289 | 0.651 | 2458 | 348 | 23738 | 4236 |

(旧口径数字取自 frozen 目录统计;新口径由 §3.1 算法(尾块参与)全量扫描得出,2026-09-15。)
