# 实验结果-G0G1G2：全局共享 KV 场景首轮机制实验（M1 引擎 + 存储感知调度策略）

| 项目 | 说明 |
|---|---|
| 日期 | 2026-09-08 |
| 范围 | 设计文档 [全局共享KV场景的存储感知调度策略与实验设计-20260908](docs/全局共享KV场景的存储感知调度策略与实验设计-20260908.md) 的 G0（不变量）+ G1（四象限×路由/动作策略族）+ G2（H×U×O 八类混合×D2 排序族）首轮机制对照；**不含** λ* 容量扫描、G3–G5 正式包 |
| 被测代码 | [sim/m1.py](sim/m1.py)（M1 迭代级引擎）、[sim/policies_g.py](sim/policies_g.py)（策略族）、[sim/experiments/g_common.py](sim/experiments/g_common.py)（S-G 场景/SLO 冻结）、[tools/g_report.py](tools/g_report.py)（图表） |
| 测试状态 | `.venv/bin/python -m pytest tests/ -q`：**65 项全绿**（37 既有 + 28 本轮新增，覆盖解析一致/守恒/依赖/容量/退化/确定性/排序键） |
| 复现命令 | `.venv/bin/python -m sim.run --exp g1 --seeds 5 --duration 300`；`--exp g2` 同；`.venv/bin/python tools/g_report.py` |
| 种子与统计 | 每格 **5 种子**（0–4），配对差值中位数 + percentile bootstrap 95% CI（1000 次）；**机制级首轮，非纲领 §11.2 的 10+ 种子冻结验收矩阵** |
| 模型状态标签 | decode 开启、缓存冻结（`local_cache_gb=0`，S-G）、副本双 mem、W=4/K=2、prefill 曲线轻微超线性（G_TABLE，机制级占位） |

## 总判决

**阶段性 go（限首轮机制层）。** 三个 headline：

1. **动态存储信息存在增量，且静态信息拿不到**：I/O 紧象限下，逐请求动态取算（D2b，quote 实时估计 `Ê_fetch` vs 重算成本）相对强基线 B-Hist（EWMA 历史 + 在途记账）把 P95 TTFT 从 0.733s 降到 0.582s（**−21.6%，配对 CI [−0.170,−0.108] 全负，5/5 种子获胜**）；而同一动作空间、只用 nominal 静态带宽的 E-StaticAR 仅 −0.015s（CI 跨零）。信息增量来自"当前拥塞下的取出耗时"，不是动作空间本身。
2. **双紧象限反转：个体最优 ≠ 全局最优**：同一 D2b 在"双紧"象限相对 B-Hist **+0.372s（0/5 种子）**——每个请求看到拥塞都改重算，集体把重算洪峰压到本已 89% 利用率的 GPU 上，所有类别达标率齐跌（S3 类 0.97→0.73）。True-State 诊断（无噪声、零延迟真值反馈）同样 +0.215s——**更准的个体估计不解决集体踩踏，缺口在协同而非信息精度**（直接验证纲领 R5 判读）。
3. **排序维度在此配置无空间**：G2 中 E-LPM / E-Prio / D2a / D2c 与 E-FCFS 逐位相同（Δ=0.000）——因为引擎按设计内建了"等待 KV 的请求不阻塞 ready 请求"（纲领 §3.2），且该负载下 prefill 预算与 HBM 不构成准入竞争；收益全部集中在**动作选择**（fetch/recompute，D2b −26.6%）与**源选择**（D1b：io −0.119s、both −0.136s、5/5，且是双紧象限唯一稳定为正的策略）。

边界内结论（适用范围）：S-G 场景（全局可见、无本地缓存）、5 种子、固定负载 λ=2.5/2.2、上述参数档位；低负载（双松）象限按设计预期无收益（负控制通过）。

---

## G0：M1 引擎与不变量（前置门槛）

新增 28 项单元测试（[tests/test_m1.py](tests/test_m1.py)），对应设计 G0 交付与纲领 §11.1 的 M1 相关行：

| 测试组 | 验证的性质 |
|---|---|
| TestAnalyticSingleRequest（3） | 单请求无竞争：fetch 链路 `路径+t_base+B/b×2段`、重算路径、跨迭代切块（预算 256 切 700 token）完成时间与手算**逐项吻合**（abs≤1e-9） |
| TestConservation | 存储守恒：双 node mem 与 fabric 的 served 字节 == Σfetch 字节；全部请求完成、`tok==O`、首 token 在取回之后 |
| TestCapacityAndDiscipline | HBM 保守预留峰值 ≤ 容量；S-G 下动作集合 ⊆ {fetch, recompute, prefill}、本地缓存恒空 |
| TestDegeneration | live 无噪声时 quote 估计 == nominal 静态成本（"quote 退化为 nominal 复现静态决策"不变量） |
| TestOrderKeys | fcfs/lpm/prio/ready 排序键单调性 |
| TestDeterminism | 同种子两次运行 summary 逐字节相同 |
| TestSGScenario | 象限背景与双副本目录装配正确 |

**判读**：按设计"G0 不通过不进入机制跑分"，本关已过。开发过程中冒烟暴露并修复了三个问题（准入死锁：running 空时从未调用 `_admit`；门控未按策略开关条件启用；估计器不对称：路由器重算成本未除以 GPU 速率）——均先有不变量/区分度异常、后定位修复，符合"冒烟检查区分度"流程要求。

---

## G1：四象限压力 × 路由/动作策略族

**场景**。W=4 worker、K=2 存储节点（mem 60GB/s×2，双副本）+ 共享 fabric 120GB/s；负载 5 类（S3/L1/S1/L3/M0，长短命中×长短后缀×长短输出四角 + 无命中对照，λ=2.5，300s，cohort≈675）；压力象限由背景负载构造：loose（mem 8/fabric 12/GPU 0.10）、io（42/78/0.10）、gpu（8/12/0.30）、both（42/78/0.30）。SLO 合成档位（公式冻结）：S3 0.25 / L1 0.54 / S1 0.57 / L3 1.00 / M0 0.26s（TTFT），TPOT 0.029s。

**策略**（11 个，全部 E-FCFS 排序）：B-RR / B-LL / B-DynKV / B-Moon-S / B-Hist（强基线）/ Hist+Joint（I1+动作空间）/ E-StaticAR（I0+动作空间）/ D1b 源选择 / D2b 动态取算 / D1a 联合 / True-State 诊断（live 无噪声）。

![G1 P95 TTFT](docs/figures/fig_g1_ttft.png)

**读法**。四象限各一栏，条高 = 5 种子 P95 TTFT 中位数，误差线 = 种子 min–max。loose 象限所有策略贴在一起（0.326–0.389s）——设计预期的无收益负控制区，通过。io 象限出现清晰分层：动态动作三策略（D2b/D1a/True-State，0.582）< Hist+Joint（0.600）≈ D1b（0.603）< B-RR（0.617）< B-LL 族（0.705）< B-Hist（0.733）。gpu 象限回到齐平（0.386–0.452）。both 象限方向反转：D1b（0.669）< B-RR（0.676）< E-StaticAR（0.764）< B-Hist（0.805）≪ D1a（0.962）< D2b（1.147）。

### 表：G1 各象限 P95 TTFT 与配对差（vs B-Hist）

（完整 11 策略×4 象限表由 `tools/g_report.py` 生成；此处列关键行，Δ 为同种子配对差中位数 [bootstrap 95% CI]，胜数 = 差为负的种子数）

| 象限 | 策略 | P95 TTFT(s) | Δ vs B-Hist [CI] | 胜/种子 | 达标率 | min 类 | fetch 占比 |
|---|---|---:|---|---:|---:|---:|---:|
| io | B-Hist（参照） | 0.733 | — | — | 0.925 | 0.69 | 1.00 |
| io | E-StaticAR（I0+动作） | 0.712 | −0.015 [−0.029,+0.003] | 4/5 | 0.919 | 0.71 | 0.35 |
| io | **D2b 动态取算** | **0.582** | **−0.158 [−0.170,−0.108]** | **5/5** | 0.924 | **0.86** | 0.00 |
| io | D1a 联合 | 0.582 | −0.158 [−0.170,−0.108] | 5/5 | 0.924 | 0.86 | 0.00 |
| io | True-State 诊断 | 0.582 | −0.158 [−0.170,−0.108] | 5/5 | 0.924 | 0.86 | 0.00 |
| io | D1b 源选择 | 0.603 | −0.119 [−0.184,−0.090] | 5/5 | **0.964** | 0.86 | 1.00 |
| both | B-Hist（参照） | 0.805 | — | — | 0.888 | 0.64 | 1.00 |
| both | E-StaticAR | 0.764 | −0.025 [−1.003,−0.020] | 5/5 | 0.890 | 0.67 | 0.35 |
| both | **D1b 源选择** | **0.669** | **−0.136 [−1.073,−0.105]** | **5/5** | **0.927** | 0.77 | 1.00 |
| both | D2b 动态取算 | 1.147 | **+0.372 [+0.075,+5.065]** | 0/5 | 0.720 | 0.54 | 0.01 |
| both | True-State 诊断 | 0.990 | +0.215 [+0.107,+13.461] | 0/5 | 0.713 | 0.57 | 0.00 |

![G1 达标率](docs/figures/fig_g1_attain.png)

**读法**。条 = 各类达标率均值，黑色短划 = 最差类。io 象限 D2b/D1b 把 min 类从 0.69/0.64 拉到 0.86/0.77；both 象限 D2b 反而把 min 类打到 0.54。

![G1 资源构成](docs/figures/fig_g1_resource.png)

**读法与资源证据**（左：GPU 时间构成——必要 prefill / 重复重算 / decode / 空闲，短划标出其中"等 KV 空转"；右：存储前台带宽利用率）：

- **io 象限的收益来源是"并行资源互换"**：B-Hist 族 fetch P95 0.69s、fabric 前台利用率仅 0.036（背景占大头）、GPU 等 KV 空转 1.0s；D2b 用 59.2s 的重复重算（dup）换掉全部取回等待（kvwait→0、fabric 前台→0.000）——把串行等待的存储队列时间换成可与 decode 并行的 GPU 计算。这正是 §3.3 两请求算例"贵的取、便宜的算"在系统级的放大。
- **both 象限的反转是协调失效**：GPU 背景负载 0.30 时 decode 已占 ~89% 容量，D2b 再压入 73.9s 重复重算，prefill 队列变长，逐类达标率全部下跌（L1 0.60→0.54、M0 0.94→0.73、S1 1.00→0.83、S3 0.97→0.73）。E-StaticAR"不反应"反而稳定（−0.025，5/5）：nominal 估计看不见拥塞、继续取回，取回与 GPU 各自并行流动。**个体视角的 quote 拥塞项在集体层面变成踩踏信号**——需要协同（影子承诺/门控换向），即 G5/R5 主线。
- **D1b 是双紧下唯一稳健策略**：worker 仍按负载选，但源选择按 quote 估计 + 近并列随机化分散到两个节点（fetch P95 0.69→0.46–0.47），不改动作、不增 GPU 负担，io/both 两象限均 5/5 为正。它也印证设计 §3.1 的退化推论：S-G 下 worker 选择无信息可用（B-LL ≡ B-DynKV ≡ B-Moon-S 三者逐位相同），存储状态的 D1 入口只剩源选择（D1b）与联合动作（D1a）。
- **B-RR 意外强于 B-LL**（io 0.617 vs 0.705）：LL 按 wait_est 聚堆导致负载集中，RR 均匀铺开——主流 least-loaded 在此配置的已知弱点（生产上用 power-of-two 缓解），报告保留该现象。
- **True-State ≡ D2b/D1a（io 象限逐位相同）**：采样/噪声不是当前收益的限制，动作空间才是；both 象限 True-State 与 D2b 同败，进一步说明瓶颈在协同。

![G1 时间线](docs/figures/fig_g1_timeline.png)

**读法**。上两行（io 象限）：B-Hist 的 GPU 迭代里 prefill/decode 混合并伴随 fabric 队列波动；D1a 切换为重算后 fabric 前台清空、GPU prefill 密度上升但 decode 主导不变。下两行（both 象限）：B-LL 的 HBM 预留缓慢爬升、队列稳定；D2b 的重算洪峰使 HBM 预留与等待队列同步堆积。

---

## G2：H×U×O 八类混合 × D2 排序策略族

**场景**。纲领 §7.2 第二正交组全 8 类（H∈{512,16K} × U∈{512,8K} × O∈{128,2048}，等份额 1/8，λ=2.2，300s，cohort≈594）；路由固定 B-Hist（排序族）/对照含动作策略；象限 io / gpu / loose。

**策略**（9 个）：E-FCFS(B-Hist) / E-LPM / E-Prio / D2a 就绪排序 / D2c 预算排序 / D2d 门控 / D2b 动态取算 / D2b+D2d 组合 / D1a 联合。

![G2 策略对比](docs/figures/fig_g2_policy.png)

**读法**。io 象限：五个排序策略（FCFS/LPM/Prio/D2a/D2c）完全重合（1.076s），D2d 门控单独变差（1.238s），D2b/组合/D1a 降到 0.796s。gpu 象限：全体接近（0.46–0.49），D2b 略优。loose：D2b 仍 −0.027（5/5，CI 全负——死区类重算免取回延迟的微小红利）。

### 表：G2 各象限（vs E-FCFS(B-Hist)）

| 象限 | 策略 | P95 TTFT(s) | Δ [CI] | 胜/种子 | min 类 | fetch% |
|---|---|---:|---|---:|---:|---:|
| io | E-FCFS（参照） | 1.076 | — | — | 0.51 | 1.00 |
| io | E-LPM | 1.076 | +0.000 [+0.000,+0.000] | 0/5 | 0.51 | 1.00 |
| io | D2a 就绪排序 | 1.076 | +0.000 | 0/5 | 0.51 | 1.00 |
| io | D2c 预算排序 | 1.076 | +0.000 | 0/5 | 0.51 | 1.00 |
| io | D2d 门控 | 1.238 | +0.140 [−0.006,+0.194] | 1/5 | 0.51 | 1.00 |
| io | **D2b 动态取算** | **0.796** | **−0.286 [−0.308,−0.257]** | **5/5** | **0.78** | 0.00 |
| io | D2b+D2d 组合 | 0.796 | −0.286 [−0.308,−0.257] | 5/5 | 0.78 | 0.00 |
| gpu | D2c 预算排序 | 0.488 | +0.000 [−0.012,+6.826] | 2/5 | **0.95** | 1.00 |
| gpu | D2b 动态取算 | 0.464 | −0.025 [−0.050,+0.750] | 4/5 | 0.96 | 0.52 |
| loose | D2b 动态取算 | 0.355 | −0.027 [−0.034,−0.008] | 5/5 | 0.99 | 0.51 |

![G2 逐类对比](docs/figures/fig_g2_class.png)

**读法（gpu 象限逐类）**。D2c 预算排序把弱势类（h0k_u8k 0.985→1.00、h16k_u0k 0.959→0.986）小幅拉平——SLO 紧迫度排序的公平性红利在计算瓶颈下可见，但幅度 <2 个百分点。

![G2 动作分布](docs/figures/fig_g2_action.png)

**读法**。D2b 的 H×U 取回矩阵：loose（左）只有 H=16K 行取回（H=512 行落入 §3.1 的 `ΔC≤ℓ` 死区——短命中不值得取）；io（右）全部翻转为重算。**逐类明细里的代价转移**（io 象限）：E-FCFS 下 h16k 类达标率仅 0.51–0.55（大 KV 取回排队），D2b 把它们拉到 0.78–0.98，但 h0k 小类从 0.99 跌到 0.84–0.87——重算洪峰的外部性由未受益类承担（设计 §1.1 条件 3"改变的是系统资源分配"的实测注脚）。

**E-LPM herd 假设：未成立（负结果）**。设计 G2 预注册的假设"lpm 优先放大命中 → 大取回扎堆 → 恶化尾部"在本配置下不成立，E-LPM 与 E-FCFS 逐位相同。机制归因：(a) fetch 在到达时提交，与 waiting 排序解耦；(b) 引擎内建非阻塞准入（等待 KV 的请求不占预算，后续 ready 请求照常进批）；(c) 该负载下 prefill 预算与 HBM 都不构成准入竞争，排序无差异可做。**排序策略要有空间，需要"排序影响资源占用时机"的配置**（如 fetch 在队首才触发、或 prefill 预算/HBM 成为准入瓶颈）——这是后续实验的场景设计输入，而非排序策略本身失效。

**D2d 门控：单独有害，组合中性**。io 象限门控 +0.140s：门控只延迟 fetch 提交而不改变动作，背景负载主导的拥塞下纯粹损失等待时间；D2b+D2d 组合中门控几乎不触发（D2b 已放弃取回），结果与 D2b 相同。门控的正确组合对象是"取回被阻时转重算"，即与动作策略耦合——留待 G5。

---

## 实现 ↔ 设计一致性检查（逐条）

按用户要求对实现与设计文档逐项核对。**一致**（关键项）：

| 设计条目 | 实现证据 |
|---|---|
| S-G-1/2 拓扑与全局目录 | `g_common.build_topo`（W=4、K=2、双 mem 副本、path_lat）；`MetadataDirectory` 全局可见 |
| S-G-3 无本地缓存 | `local_cache_gb=0`；测试 `test_no_local_action_in_sg` |
| S-G-4 动作空间 fetch/recompute | `M1Req.action`，partial 未实现（R6 范畴，设计即如此划定） |
| S-G-5 共同纪律 + 排序可插拔 | `M1Worker._loop`（decode 全量→剩余预算切 chunk）；`order_key` 五种 |
| S-G-6 信息边界 | 策略只读 `quote`/`StorageObservable`/共享 `wait_est`；True-State 为显式诊断变体 |
| §3 基线清单 | B-RR/LL/DynKV/Moon-S/Hist/E-FCFS/LPM/Prio/StaticAR 全部实现并运行 |
| §4 增强 | D1a/D1b/D1c(=dynar 的 LL 形态)/D2a/D2b/D2c/D2d(双上限) 实现；D2e 按修正口径并入默认 |
| §6 统计规则 | CRN（trace 仅由种子生成）、同种子配对差、中位数+bootstrap CI、cohort 到达窗口、未完成计入不达标 |
| SLO 冻结 | 公式 `4×(nominal取回+suffix+d0)` clip [0.25,8]，运行前固定，未按结果回调 |

**偏差与未覆盖**（均在设计划定的后续范围内，本轮明确不宣称）：

1. E-NoEvict 未单列——纲领 §5.3 第一版保守准入（按 N+O 全程预留）即 no-evict 语义，所有策略共用，无法构成对照；抢占敏感性留待后续。
2. History-Oracle 诊断、I0/I2/I3/I4 信号替换实验（`signal` 变体）未做；True-State 已覆盖"真值反馈"一档。
3. λ* 容量扫描未做（固定负载对照）；G3（Router×Engine 2×2、对称性负控制）、G4（d/τ）、G5（协同）未正式运行——G2 中 D2b+D2d 组合与 both 象限的反转现象是 G5 的直接动机。
4. W=4 单档（设计 G1 写 W∈{2,4}）；引擎纪律变体（配额制/prefill 优先）与 T_iter 曲线形状敏感性未跑（曲线为机制级超线性占位，已声明）。
5. O 为类别确定值（O_hat≡O），无输出长度预测误差维度；ITL 只报告不进达标判定。
6. 种子数 5（设计要求正式矩阵 ≥10）——本报告定位为机制级首轮，冻结验收矩阵前需扩到 10+。

---

## 与前序结论的衔接

- [M0R0](实验结果-M0R0-20260907.md) 的解析层结论（阈值交叉、两请求分配、守恒）在 M1 引擎上通过单请求解析一致测试延续；本轮把"贵的取、便宜的算"从两请求算例放大到系统级，并首次观察到其**失效边界**（双紧象限的集体踩踏）。
- 旧 E5–E9/E13–E18 的 locality/本地缓存结论在 S-G（`local_cache_gb=0`）下不适用，符合设计 §8 的预期档位；本轮 G1 中 B-DynKV ≡ B-LL 的逐位相同，首次用运行证据确认了"全局可见下 Dynamo 式路由退化为负载均衡"的设计推论。
- 纲领 v1.1 §11.3 的观察-行动表在本轮命中两行："真值贪心也差，协调后变好→主要问题是并发承诺与资源分配（R5）"与"TTFT 好、弱类受损→修改目标或增加保护约束"。

## 局限

1. 5 种子、单持续时间、固定负载；未做 λ* 与队列漂移检验，容量主张一律不外推。
2. prefill 曲线/decode 迭代参数（d0/d1/预算 4096/HBM 96GB）为机制级占位，未标定；取/算交叉位置随曲线形状移动（G_TABLE 超线性是让长命中可取的必要条件——凹曲线下取回几乎永不划算，这一敏感性已在测试中固化为线性反例断言）。
3. 存储为流体资源模型（processor sharing），无队头阻塞/对象并发效应；同前缀并发 fetch 不去重（设计 §8 第 3 条），带宽口径含重复搬运。
4. True-State 诊断的"真值"仍是估计器形态（live 无噪声 quote），非全局最优上界；不用于宣称收益上界。
5. both 象限部分策略 CI 很宽（个别种子出现秒级尾刺），中位数稳健但分布尾部未深挖——扩种子后需复查。

## 下一步（按证据依赖排序）

1. **G5 协同主线**：把 both 象限的反转作为靶子——动态取算 + 影子承诺（决策时计入"同期已提交的重算量"）或预算化取回（按 ΔC/B 价值密度分档），检验能否同时保住 io 的收益和 both 的稳健性。
2. λ* 容量扫描 + 10 种子冻结矩阵（对 io/both 两个主场景族按纲领 §11.2 验收线判 go/no-go）。
3. G3 对称性负控制与 Router×Engine 2×2（本轮 D1a≡D2b 的现象预示 worker 维度在对称拓扑下无增量，需负控制确认后收窄主张）。
4. 排序策略的场景前提检验（fetch 延迟到队首触发 / 收紧 HBM），判定 D2a/D2c 的适用条件。

## 通俗版解读（厨房比喻续）

仓库是唯一货源、每间厨房都能看到全部货架（S-G 场景）。这一轮我们给厨房装了第一块"仓库实时看板"（quote 接口），并问三个问题：

- **看板有用吗？** 有——货梯被外人占满时（I/O 紧），会看板的厨师当场改为自己重做，等第一道菜的时间缩短了两成；只背价目表的老厨师（静态信息）做不到。**但价目表+动作组合（E-StaticAR）几乎白搭**，说明关键确实是"现在"两个字。
- **看板会帮倒忙吗？** 会——货梯和灶台同时都挤时（双紧），每个厨师都看到货梯挤、都改自己重做，结果灶台被重做洪峰压垮，所有桌上菜都变慢；**就算看板零延迟零误差也一样**。缺的不是更准的看板，是大家之间的排号规则（协同）。
- **排队顺序重要吗？** 在这套规矩里不重要——因为厨房早就学会"等菜的客人不挡道"（非阻塞准入），新客随便插位也不改变谁先用灶台。顺序要有用，得先让"排在前面的占着灶台等货"成为可能。
- 最稳的赢家反而不改做法：**只换提货窗口**（D1b 源选择）——两扇窗哪扇人少走哪扇，货梯和灶台都不添乱，两种拥挤下都赚。
