# 大白话解读:共享 KV 组批与错峰调度仿真——术语直读版

> 这份文档**完全独立**,不需要配合其他文档阅读。所有术语保留原名,第一次出现时直接解释;文末附术语速查表,方便与《规格》《测试报告》对照。

---

## 0. 三句话总结

1. 这项工作按规格**实现了 `sim/cq/` 仿真器和 E19–E24 六个实验**,并用 115 个测试(含四道手算到无穷精度的金标题)验证了实现的正确性——全部通过。
2. 实验输入是 Mooncake 公开的三份真实 trace(请求到达时间、长度、前缀重复关系),不是编造的数据。
3. 最重要的发现:真实 trace 的新 token 数太大,**batch 在计算侧不省时间(G=1)**;排序类策略相对 FCFS 平均 TTFT 降低约 14%,但 MPC 没有超过最强的简单基线 SPT。

---

## 1. 研究的问题

大模型回答一个请求,要先对输入做一遍预计算(称为 **prefill**),然后才能输出第一个 token。从请求到达到第一个 token 输出的等待时间叫 **TTFT**(Time To First Token,首 token 时间),是用户体感最敏感的指标。每个请求有一个 **deadline**(截止时间,由 TTFT 的参考值乘一个系数 α 得到);在 deadline 前完成首 token 就算 **SLO 满足**(SLO = Service Level Objective,服务质量目标)。

多个请求到达时要排队、等机器。本研究问:**同样的请求流、同样的机器和存储,只改变排队和派发的规则,TTFT 和 SLO 满足率能改善多少?** 改变的规则有三个维度:

- **组批(batch)**:把 1~8 个请求合成一个 batch 一起算;
- **顺序**:空闲的 worker 先接哪些请求;
- **错峰(stagger)**:故意推迟某个 batch 的领取时刻,让读取错开。

---

## 2. 系统模型(术语逐个解释)

这一节是全文的地基,分六小节:先跟着一个具体请求走一遍完整流程,再讲存储、组批、调度器,最后逐个讲八个策略。

### 2.1 一个请求的一生

每个请求到达时带着四个关键数字:到达时间 arrival、命中前缀 **h_tokens**、新 token 数 **u_tokens**、以及由它们算出的截止时间 deadline。

- **h_tokens(命中前缀)**:这条输入的开头和某条历史请求相同的 token 数。这部分对应的 **KV cache**(预计算好的中间结果)已经存在远端共享存储里,不需要重算,但必须**读取**回来——所以 h 决定读取量。注意命中规则:只有从第 0 个 token 开始**连续相同**的前缀才算,不连续出现过的内容不算;哪怕整条输入都命中,也至少留 1 个 token 现场计算。
- **u_tokens(新 token 数)**:剩余必须现场计算的 token 数,决定计算量。

请求的一生分四步:

```text
到达 → 进入中央队列(等待) → 被选入某个 batch、被某个 worker 领取 → 逐层"读取+计算" → 得到首 token
```

**worker(推理实例)** 共 4 个,每个同一时刻只执行一个 batch;batch 从被领取到最后一层算完,期间一直占住这个 worker(就算在等数据也不能去干别的)。

执行是**逐层**的(本实验 L=32 层)。用一个放大的教学数字走一遍(真实 trace 数值小几个数量级,但结构完全相同):假设一个 batch 每层要读 4 GB、每层计算 2 秒、共享带宽 4 GB/s:

1. **第 0 层读取**:batch 被领取的瞬间,向共享存储提交一个读取流(读 4 GB),排队获得带宽;
2. **第 0 层计算**:4 GB 读完(比如 1 秒后),worker 开始算这一层,算 2 秒;
3. **第 1 层读取与计算重叠**:第 0 层计算开始的同时,就提交第 1 层的读取流——如果读取能在计算结束前传完,worker 就不用停;传不完,worker 算完就得**干等**(STALL);
4. ……如此到最后一层算完,**全体成员同时得到首 token**。TTFT = 从到达 to 这一刻的全部时间,包含中央队列里的等待。

**STALL / COMPUTE**:worker 占着但等读取,记 STALL(开着火没料炒);在计算,记 COMPUTE。STALL 高 = 瓶颈在存储读取。注意一个请求的 TTFT 由三段组成:**队列等待 + batch 内等料(STALL)+ batch 内计算(COMPUTE)**——后两段对 batch 内所有成员是相同的,这正是"短请求被长请求拖住"的来源。

### 2.2 共享存储:一条带宽怎么分

所有 worker 的读取流共用一条聚合带宽 **B**(GB/s)。规则是 **FCFS(先来先服务)**,按每个流的提交序号 `submit_seq` 排队:排最前的流先分,分到它申请的速率上限为止,剩余带宽给下一个,分完为止。举个例子:B=4 GB/s,三个流按序申请 1、4、2 GB/s,实际分到 **1、3、0**——第二个流拿不到申请的 4(只剩 3),第三个流直接饿着,直到前面的流读完退出。

每层的申请速率有讲究,这是本研究物理模型的关键机制:

- **首层流**:直接申请满速 q_max——第一批数据没到,什么都干不了;
- **下层流(lookahead)**:按"刚好赶在当前层计算结束时读完"来申请(q = 本层读取量 ÷ 当前层计算时长)——反正当前层还在算,读慢点没关系,把带宽让给别人;
- **到期提升**:到了预计读完的时刻还没读完(说明让出去的带宽亏了),申请速率提升到满速,但**排队序号不变**——不能靠提速插队。

这三个规则合起来,就产生了"错峰"的物理基础:**谁的流先提交、申请多少,直接决定共享带宽的分配顺序**;一个大批早上抢占满速,会把他后面所有流都挤到零。

### 2.3 batch:为什么可能省计算、什么时候不省

**batch(组批)** 把 1~8 个请求合成一批一起执行。收益来自计算公式:每层计算时长 c = 启动开销 t_launch + aN/η(N) + bA,其中 N = 批内新 token 总数,A 是 attention 配对数。**η(N)** 是并行效率:当 N < N_sat=512 时,批越大 η 越高(机器没喂饱,合批摊薄了启动开销、提高了利用率)。

- 所以 batch 只在"**批内新 token 少**"时省计算:比如 8 个 u=128 的请求合批,计算时间只有分开算的约 1/4(G≈4.07);
- 批内新 token 一旦超过 512,η 封顶为 1,**合批在计算侧就一分钱不省**(G=1)——这正是后面 E21 在真实 trace 上观察到的。

代价是**完整批屏障**:一个 batch 的所有成员在最后一层算完时**同时**完成。批里最快的请求也要等最慢的——短请求 TTFT 被拉长。所以组批永远是一个权衡:省计算 vs 拖累短请求。

### 2.4 调度器:什么时候决策、决定什么

中央调度器在**决策点**被唤醒:队列里有请求、且有空闲 worker 时。它输出一组**联合动作**:给每个空闲 worker 指定一个动作——**DISPATCH**(领取某个具体成员组合的 batch)或 **WAIT**(等到某个未来时刻再决策,但请求不锁定,别人随时可领)。

调度器**看不到**未到达的请求、看不到存储的真值剩余量——只能看已到达请求、worker 的公开状态、存储的周期性"报价"(每 5ms 报一次可用带宽估计)。这是刻意的:**所有策略信息边界一致,赢要赢在决策,不能赢在偷看**。

另有一条**老化保护(guard)**:任何请求在队列里等超过阈值,就会被强制安排进下一个领取的 batch,防止被排序策略饿死。

每次决策本身花时间(构建候选、推演评分)。zero 模式假设不花时间;measured 模式把真实耗时计入服务轨迹——调度越复杂,这份开销越真实。

### 2.5 八个策略逐个讲

所有策略面对同一批请求、同一个存储,差别只在"排序键 + 组批规则 + 是否等待"。先讲五个**简单规则**(排序后从队首连续填批,不做推演):

**cq_fcfs(FCFS,主基线)**——按 (到达时间, 请求号) 排序,队首开始连续往批里加,加不下就停,空闲 worker 立即领取。直觉:最公平、天然防饿死,也是 vLLM / SGLang 默认调度机制在本架构下的投影,所以永远作为第一参照。风险:队首来一个又长又急读取的请求,后面所有短请求跟着陪等(队头阻塞)。

**cq_lpm(LPM)**——按命中前缀 h 从大到小排。直觉:h 大的请求复用的 KV 最多,"读回来就省了一大截计算",优先保它;对应 SGLang 可选的 longest-prefix-match。风险:h 大意味着读取量大,优先派发会长时间占用共享带宽,挤别人。

**cq_spt(SPT)**——按 T0 从小到大排(T0 = 该请求单独执行的参考服务时间,短任务优先)。直觉:排队论的经典结论——短任务先做,平均等待下降;本次冒烟中它是**最强的简单基线(B\*)**。风险:长任务一直被插队(靠 guard 兜底)。

**cq_edf(EDF)**——按 deadline 从早到晚排。直觉:截止时间最紧的最先做,实时调度的经典规则。风险:deadline 紧但工作量大,和 deadline 松但工作量小的相比未必该先做——它不看"还剩多少活"。

**cq_slack(Slack)**——按 slack = deadline − 当前时刻 − 预计服务时间 从小到大排。直觉:修正 EDF 的盲点——不只看截止时间,还看"做完手里的活之后还剩多少富余",富余最小的最紧迫;这是 Cascade 论文预算驱动思路的代理实现。风险:预计服务时间有估计误差时排序会错。

**cq_pair(Complement)**、**cq_local(Local)**、**cq_mpc(MPC)** 这三个结构更复杂,单独放到下一小节详解。

**为什么要第二个参照 B\***:只赢 FCFS 不说明复杂策略有用——可能随便一个认真挑过的排序就到顶了。B\* 是在训练集上从所有简单规则里冻结选出的最强者(本次冒烟中是 SPT);**相对 B\* 的收益才是"复杂调度"的真实增量**。

### 2.6 三个进阶策略详解

这三个策略的关系一句话概括:**cq_pair 是"聪明的组批",cq_mpc 是"会推演的调度",cq_local 是 cq_mpc 的对照组**。逐个讲。

#### cq_pair:把"吃带宽的"和"吃计算的"配成一锅

先定义一个量:**r = 读取量 ÷ 服务时间**。它衡量一个请求"多吃带宽"——h 大 u 小的请求 r 大(要把大量 KV 搬回来但很快算完),h 小 u 大的请求 r 小(几乎不取料但要算很久)。

cq_pair 的机制:以**最老请求为锚**(保证最老的不被跳过),其余请求按"互补距离"排序——挑 r 与锚互补的:锚的 r 大,就优先配 r 小的,使得配出来的对子平均读取压力落在全体请求的平均水平(训练集几何均值 r_ref)附近。

为什么这样配可能有用?看一个数字例子。带宽 B=2 GB/s,有两类请求:

- 请求 A(重读轻算):每层读 4 GB、算 2 秒,r = 2;
- 请求 B(轻读重算):每层读 1 GB、算 6 秒,r ≈ 0.17。

把两份 A 配成一批:每层读 8 GB、算 4 秒——读取需要 8÷2 = 4 秒,恰好把计算窗口填满,一点余量都没有;而 A+B 混批:每层读 5 GB(2.5 秒传完)、算 8 秒,读取宽裕地藏在计算里。更关键的是**批与批之间**:系统有 4 个 worker 共享一条带宽,如果每个批都是"双 A"这种高读取压力的,4 条流在 FCFS 队列里互相挤,排在后面的批直接拿不到带宽、长时间 STALL;批的读取压力有高有低、平均贴近容量,整体排队才顺畅——这相当于**被动版的错峰**(错峰是主动挑时机,cq_pair 是靠组批搭配让冲突自然变少)。

风险:它只调整"谁和谁一锅",不调整"什么时候领取";r_ref 依赖训练数据标定;而且如果负载里所有请求长得差不多(后面会看到真实 trace 大致如此),没有互补空间,它就退化成到达序。

#### cq_local:MPC 的对照组,故意"看不见"带宽竞争

cq_local 和 cq_mpc 用**完全一样**的候选生成、完全一样的评分目标、完全一样的尾评估规则,唯一的差别藏在推演环节:**cq_local 推演时假设每个 worker 独享整条带宽**——4 个 worker,就当作有 4 条互不干扰的 B。

为什么这个"故意错"的设定有价值?看例子:两个大批、两个空闲 worker。cq_local 推演:两批各享一条带宽,读取得满分、互不影响,评分很高,于是两个都立即派发。但真实执行时两条流挤同一条 FCFS 带宽,后提交的那批可能长时间拿不到带宽、严重 STALL——**cq_local 高估了自己的方案**。cq_mpc 推演时看到了这个挤压,可能让第二批 WAIT 一下、或改组别的批。

所以 cq_local 在研究中的角色是**消融实验(ablation,即控制变量地拆掉一个部件)**:

> cq_mpc 的成绩 − cq_local 的成绩 = "评分时建模跨 worker 带宽竞争"这件事本身的净价值。

差值大 → 共享带宽干扰是关键、值得花成本建模;差值≈0 → 瓶颈根本不在带宽竞争(比如在计算),用便宜的 local 版就够了;差值为负 → 建模引入的复杂度反而有害。注意一个细节:cq_local 只是在**评分时**假装带宽独立,**真实执行仍然走共享带宽**——错只错在它对后果的想象,这正是要测量的误差。

#### cq_mpc:每步都把"之后会怎样"完整推演一遍再落子

MPC = Model Predictive Control(滚动时域预测控制)。类比开车用导航:每次都让导航规划出到达终点的完整路线,但**只按它开下一小段**,遇到新路况就重新规划——不指望一次规划永远正确,而是靠频繁重规划适应变化。

cq_mpc 在每个决策点做五件事:

1. **重建预测世界**:从允许看的信息(已到达请求、worker 公开状态、存储最新报价)重建一个当前系统的近似副本——带宽取最新报价、各流剩余字节取账本记录、计算剩余按画像公式估计;
2. **算保底方案**:先算出"什么都不折腾、按 EDF 规则"会怎么做(基础动作);
3. **生成候选**:对每个锚请求按三种顺序(形状相似 / 读算互补 / 截止紧迫)尝试组批,保留所有中间形态的批;再生成各种 WAIT 时刻(比如参考时长的 5%、10%、20% 之后);
4. **逐个推演**:对每个候选动作,在预测世界里模拟"执行它,然后所有已到达请求按 EDF 规则一直跑到全部完成",得到每个请求的完成时间,算出目标值(SLO 失败数 / 总 TTFT / 归一化 TTFT,按当前实验目标选);
5. **选优并过门槛**:目标最好的候选胜出;但若它不比保底方案好出一定门槛(gate),就执行保底方案——防止被预测里的噪声差异带偏,为一个想象出来的好处瞎折腾。

**WAIT 为什么要靠推演而不是拍脑袋**:等待有两面。收益面——等 50ms 可能凑出更优的批,或者等某个正在读取的流传完、新批不被挤在它后面;代价面——等待期间 deadline 在流逝,排队的请求在变老。这两面哪个大,没有先验答案,只能把"等"和"不等"都推演到全部完成、比总效果——这正是 MPC 存在的意义:**用推演代替猜测**。

**成本与退路**:一次决策要推演几十个候选×完整排空,有 2ms 预算;超预算的候选不再更新最优;连保底方案都推不完就直接返回保底方案(fallback)。E24 那个发现——MPC 的收益从决策延迟约 2ms 起被侵蚀——说的就是这笔成本开始反噬。

**三个已知局限**(看它的成绩时要记住):推演里**没有未来到达**(只把已到达的请求排空,50ms 后会来什么它不知道);带宽用的是**报价估计**(估错则推演整体偏);每次只执行第一个动作(规划两步、走一步)。


---

## 3. 数据:三份 Mooncake trace

Mooncake(开源 KV cache 系统)公开发布了三份 trace,每行是 (timestamp 毫秒, input_length, output_length, hash_ids)。hash_ids 描述这条输入的 512-token 块序列,用来判断前缀命中。三份文件都做了 SHA256 校验:

| 文件 | 请求数 | 说明 |
|---|---:|---|
| conversation_trace | 12,031 | 对话流量 |
| toolagent_trace | 23,608 | 工具代理流量,前缀重复最多(约 40.8% 的 token 可命中) |
| synthetic_trace | 3,993 | **Mooncake 官方构造的合成 trace**,不是真实流量 |

处理方式:每份文件按时间切成三段——前 20% 的请求用来**建缓存目录**(frozen_history_catalog:它们的完整块预先驻留在共享存储,运行期间不变);中间 20% 是训练段;后 60% 是评价段。这样做保证所有策略面对**完全相同的命中机会**,不会因为某策略完成得快就获得更多缓存命中。

trace 里的时间戳按目标负载缩放(ρ = 负载 ÷ λ0,λ0 是该文件训练段算出的容量基准:conversation 6.68、toolagent 14.56、synthetic 8.66 请求/秒),token 数和命中关系不变。

---

## 4. 做了什么:模拟器,以及为什么不是真实系统

新增了独立的 `sim/cq/` 仿真器(12 个模块)和 6 个实验入口。**没有使用真实 GPU**:每个 batch 的每层计算时长由公式 synthetic-v1 给出(参数包括 N_sat=512、t_launch=50μs 等),读取按共享带宽 FCFS 分配精确推进。原因:① 公平——只让调度规则变,其他全部钉死;② 可复现——同种子同结果;③ 便宜。代价:所有绝对时间是合成机制结果,只能用于策略之间的**相对**比较,不构成硬件结论。

**正确性验证(四道保险)**:

1. **四计划金标(P1–P4)**:构造 4 请求、2 worker、2 层的极小场景,所有完成时刻手工用分数推演(例如 5021/576 秒),要求模拟器 Fraction 严格相等复现。四道全部通过,其中 P4 专门检验"同刻时旧 batch 的下层读取提交先于新 batch 的首次提交"这条规则——把顺序故意写反,P4 立即不通过。
2. **枚举/B&B 对拍**:对小场景求理论最优,穷举和带剪枝的分支定界两种独立实现,答案完全一致,且 B&B 给出下界=上界的证书。对拍抓出过一个真实的时刻顺序 bug。
3. **50 个新单元测试**:单位换算、FCFS 分配、到期提升、零读取/零计算边界、信息边界(策略拿不到未到达请求和存储真值)、读取字节守恒(两个策略总量相等)、右删失统计口径等,全部通过;旧系统 65 个测试零回归(共 115 passed)。
4. **trace 导入指纹**:三份文件用上述缓存目录规则推出的统计(建目录请求数 2121/4249/744、命中比例等)与规格预先手算的验收表逐位一致。

---

## 5. 六个实验各发现了什么

以下数字全部来自**冒烟级**运行(训练段时间块、决策零成本、4 策略子集)。正式评价(15 个评价块、8 策略、measured 成本)尚未运行,所以这些都是阶段性读数,不是最终结论。

### E19:trace 与计算画像的可信度

核对三份 trace 的完整性、命中分布、容量覆盖;验证 synthetic-v1 计算公式。结果:SHA256 全部匹配;trace-wide 能力组(262144 token / 128 GB)下 singleton 可行率 100%;公式抽查值与规格表逐位一致。没有实测硬件画像,`hardware_status=not_available`。

![E19 trace 概览](docs/figures/cq_fig_e19_trace_profile.png)

**看什么**:上排是三份 trace 的到达间隔分布(对数纵轴),下排是每请求 h(命中)与 u(新 token)的联合分布——三份负载形状差异很大,这是后续结论的前提。

![E19 组批计算曲线](docs/figures/cq_fig_e19_g_curve.png)

**看什么**:G_batch = 成员单独计算时间之和 ÷ batch 计算时间,>1 表示组批省计算。只有 u 小的请求(HS_US,一 batch 8 个)G≈4.07;u 大的(HL 类)G≈1——预告了 E21 的发现。

### E20:小场景的理论最优与在线策略差距

四道金标复现(全过);在 4 请求小场景上对 M(makespan)/S(失败数)/T(总 TTFT)/R(归一化 TTFT)四个目标分别求网格最优。n=4 的实例枚举与 B&B 一致、证书闭合;n=6 按规格保留 time_limit(超时不冒充通过)。

一个非直觉结果:网格最优 M=8.0,优于四个手算计划里最好的 P3(≈8.72)——**在领取时刻可以精确选择的设定下,合适的错峰比大小混批更好**。在线策略(不知道未来到达)在此场景离最优约 34%,这个差距就是"预知未来"的价值上限。

![E20 四计划金标](docs/figures/cq_fig_e20_four_plan.png)

**看什么**:四个计划的 M 与平均 TTFT(左)、STALL(右)。P4(错峰)平均 TTFT 最短、STALL 最少,但 M 最大——收益与取舍并存。

![E20 gap 与求解时间](docs/figures/cq_fig_e20_gap_runtime.png)

### E21:组批收益与同步损失

用真实 trace 的形状(每块前 32 条请求,arrival 全设 0)做有限工作集实验,对比三种能力档(A: n_max=1 禁止组批;B: 同 h-u 中位数象限才能合批;C: 自由)× 两种 profile(默认 / no_speedup)。

**核心发现:真实 trace 形状下 G_total = 1.00——batch 在计算侧完全不省时间。** 原因:真实请求的 u(几百到 125,683)远超 N_sat=512,η 恒为 1。对照:A 档(禁止组批、纯排序)下 SPT/EDF/MPC 的 SLO 满足率 0.961 vs FCFS 0.477——**排序本身的收益很大,而组批没有增量**。

![E21 组批权衡](docs/figures/cq_fig_e21_batch_tradeoff.png)

**看什么**:各策略的设备计算秒几乎相同(G=1 的体现),差异全在短请求 TTFT / SLO 上。

### E22:错峰的因果隔离

两层证据:(A) 确定算例——成员固定,只改 LL 的领取时刻(0 到 1 秒,带宽 2/4/8),记录完整的提交序号与每段速率;核对数值。(B) 真实 trace 固定批工作集——按 FCFS 预先分好 batch 目录,只比较领取顺序(immediate / order / wait),逐请求验证恒等式 **ΔTTFT = Δqueue + Δcompute + Δstall** 且固定成员的 **Δcompute = 0**(断言级验证,证明成员没被改动)。

结果:B=4 时领取延迟从 0 到 1.0,STALL 从 3.00 降到 2.875(−4.2%),M 从 11 升到 11.375(+3.4%)——**错峰降低等料,代价是总完成时间变长**,与解析示例一致。

![E22 时间线](docs/figures/cq_fig_e22_timeline.png)

**看什么**:右图每个横条是一个 worker 的时间线,蓝=COMPUTE、橙=STALL。错峰方案(P4)橙色总量少、整体更长。

![E22 等待分解](docs/figures/cq_fig_e22_wait_decomposition.png)

**看什么**:改动带来的 TTFT 变化拆成 Δqueue 和 Δstall 两部分——如果 STALL 降多少、中央排队就涨多少,说明等待只是换了地方,不是真收益。

### E23:主收益矩阵(冒烟)

2 文件 × B{20,80} × ρ{0.6,0.9} × θ{S,T} × 4 策略 × 2 训练块 = 128 runs,同 trace 配对比较。配对中位数:

| 策略 | SLO 满足率 (pp) | 平均 TTFT (%) | 相对 B\*(=spt) |
|---|---:|---:|---:|
| cq_spt | **+1.00** | **−14.40** | —(它就是 B\*) |
| cq_slack | +0.66 | −14.23 | ≈0 |
| cq_mpc | +0.78 | −14.26 | −0.03%(≈0) |

冒烟训练段上 B\* = SPT;**MPC 的全部收益与最强简单规则打平,没有额外增量**。两个负收益格点如实保留:synthetic B=80/ρ=0.9 下 FCFS 满足率(0.491)高于 SPT/MPC(0.478);E21 的 B 档下 MPC(0.242)低于 FCFS(0.469)。

![E23 收益总览](docs/figures/cq_fig_e23_gain_overview.png)

**看什么**:各策略相对 FCFS 的配对中位数收益,柱子在零线以上为正。这是训练段数据,正式结论须用评价段重跑。

![E23 负载曲线](docs/figures/cq_fig_e23_slo_load.png)

**看什么**:左图 FCFS 的 SLO 满足率随 ρ 上升而下降;conversation 在两种带宽下都已接近崩溃(读取带宽瓶颈),synthetic 有清晰坡度——实验区间选在 20%~95% 满足率之间,有区分度。

### E24:信息、观测与控制开销(单因素扫描)

固定一个重载格点(B=20, ρ=0.9, α=4, θ=S),每次只改一个因素。主要发现:

- 信息来源:σ=0 且 B 恒定时 history 与 current 等价(正确行为);
- **控制开销:MPC 的 SLO 收益从决策延迟约 2ms 开始被侵蚀(0.1667 → 0.1621),FCFS 不受影响**——给出了"何时退回简单规则"的第一条界线。

![E24 鲁棒性](docs/figures/cq_fig_e24_robustness.png)

**看什么**:四张小图分别是信息来源、采样周期、控制延迟、搜索深度对 SLO 满足率的影响。重点看第三张里 MPC 那条线在 2ms 后掉头。

---

## 6. 知道了什么、还不知道什么

**已确认(校准过的)**:模拟器、求解器、trace 导入的正确性都有硬证据;真实 trace 形状下组批无计算收益(G=1),调度价值在顺序与时机;好的简单排序(SPT)带来约 14% 的 TTFT 改善;MPC 未超过 B\*,且其收益对决策耗时敏感。

**未确认(下一步)**:正式 train(全 5 训练块冻结 B\*/gate)与 eval(15 评价块、8 策略、measured 成本两套)未运行;带宽噪声 σ、计算偏差 e、非平稳 B、协议对照等 E24 面板未实现;无实测硬件画像。**当前没有 go/no-go 判定,没有策略获胜的结论。**

---

## 附:术语速查表

| 术语 | 一句话解释 |
|---|---|
| TTFT | 请求到达到输出第一个 token 的时间 |
| SLO / deadline | 服务质量目标;本实验 deadline = arrival + α·T0,按时完成即满足 |
| T0 | 该请求单独执行(空系统、参考带宽)的服务时间,用于定 deadline 和排序 |
| h_tokens / u_tokens | 命中前缀 token 数(决定读取量)/ 新 token 数(决定计算量) |
| KV cache | 预计算好的中间结果,存远端共享存储,命中部分读取后不必重算 |
| worker | 推理实例,共 4 个,同时各执行一个 batch |
| B / q_max | 聚合存储带宽 / 单流申请速率上限(GB/s) |
| FCFS(存储) | 按 submit_seq 排队分配带宽:前面的先分满 |
| lookahead / 到期提升 | 下层流按"赶在计算结束时读完"申请低速;到期没读完提速但序号不变 |
| STALL / COMPUTE | worker 占用但等读取 / 正在计算 |
| batch / 完整批屏障 | 1~8 个请求合成一批;成员同时完成,短请求被拖住 |
| G_batch | 成员 singleton 计算时间之和 ÷ batch 计算时间,>1 表示组批省计算 |
| 组批 / 排序 / 错峰 | 调度的三个自由度:合哪些请求、先派谁、何时派 |
| FCFS(策略) | 到达序组批立即派发,vLLM/SGLang 默认机制的投影(主基线) |
| SPT / EDF / slack | 短任务优先 / 早截止优先 / 剩余富余时间小者优先 |
| MPC | 滚动预测调度:推演候选动作的完整排空结果再选首动作 |
| r(读取压力) | cq_pair 用:读取量 ÷ 服务时间,衡量请求多吃带宽;配对让批的平均压力贴近全体均值 |
| 消融(ablation) | 控制变量地拆掉一个部件来测它的价值;cq_local 即 cq_mpc 拆掉"带宽竞争建模"的版本 |
| 决策点 / 动作 | 队列非空且有空闲 worker 时调度器被唤醒;动作 = DISPATCH(领取某批)或 WAIT(等到某时刻) |
| guard(老化保护) | 请求等待超过阈值时强制进入下一个领取的 batch,防饿死 |
| fallback | MPC 超出决策预算(2ms)时退回基础动作(EDF) |
| B\* | 训练集冻结的最强简单基线(本次冒烟中为 SPT) |
| λ0 / ρ | 该 trace 训练段算出的容量基准到达率 / 实际负载与 λ0 之比 |
| frozen_history_catalog | 用 trace 前 20% 请求建好的固定缓存目录,运行期不变,保证命中机会对等 |
| zero / fixed / measured | 控制开销模式:决策不耗时 / 固定延迟 / 实测耗时计入轨迹 |
| makespan (M) | 有限工作集全部请求完成时间 |
| 右删失 | 未完成请求只知 TTFT 下界(截断时刻 − arrival),仍计入分母 |
| 冒烟 (smoke) | 小规模验证运行;正式评价前所有实验数字均为冒烟读数 |
