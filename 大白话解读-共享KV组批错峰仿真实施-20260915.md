# 大白话解读:共享 KV 组批与错峰调度仿真——术语直读版

> 这份文档**完全独立**,不需要配合其他文档阅读。所有术语保留原名,第一次出现时直接解释;文末附术语速查表,方便与《规格》《测试报告》对照。

---

## 0. 三句话总结

1. 这项工作按规格**实现了 `sim/cq/` 仿真器和 E19–E24 六个实验**,并用 115 个测试(含四道手算到无穷精度的金标题)验证了实现的正确性——全部通过。
2. 实验输入是 Mooncake 公开的三份真实 trace(请求到达时间、长度、前缀重复关系),不是编造的数据。
3. 最重要的发现:真实 trace 的新 token 数太大,**batch 在计算侧不省时间(G=1)**;排序类策略相对 FCFS 平均 TTFT 降低约 14%,但 MPC 没有超过最强的简单基线 SPT。

---

## 1. 研究的问题

大模型回答一个请求,要先对输入做一遍预计算(称为 **prefill**),然后才能输出第一个 token。从请求到达到第一个 token 输出的等待时间叫 **TTFT**(Time To First Token,首 token 时间),是用户体感最敏感的指标。每个请求有一个 **deadline**(截止时间,由 TTFT 的参考值乘一个系数 α 得到);在 deadline 前完成首 token 就算 **SLO 满足**(SLO = Service Level Objective,服务质量目标)。

多个请求到达时要排队、等机器。本研究问:**同样的请求流、同样的机器和存储,只改变排队和派发的规则,TTFT 和 SLO 满足率能改善多少?** 改变的规则有三个维度:

- **组批(batch)**:把 1~8 个请求合成一个 batch 一起算;
- **顺序**:空闲的 worker 先接哪些请求;
- **错峰(stagger)**:故意推迟某个 batch 的领取时刻,让读取错开。

---

## 2. 系统模型(术语逐个解释)

**请求的输入**。每个请求有两类工作量:

- **h_tokens(命中前缀)**:输入开头与历史请求相同的 token 数。这部分对应的 **KV cache** 已经算过、存在远端共享存储里,不需要重算,但要从存储**读取**回来——读取量决定于 h。
- **u_tokens(新 token 数)**:剩余必须现场计算的 token 数——计算量决定于 u。

一份请求记录 = (到达时间 arrival, 输入长度, 与历史哪些请求共享前缀)。

**KV cache 与命中规则**。只有从第 0 个 token 开始**连续相同**的前缀才算命中;不连续出现过的内容不算。每单至少留 1 个 token 现场计算(即使整条输入都命中)。

**worker(推理实例)**。共 4 个,每个同一时刻只执行一个 batch;batch 从领取到最后一层计算结束期间占住 worker。

**执行结构**。每个 batch 逐层执行(本实验 L=32 层):每层先把该层要读的 KV 读回来(读取量 = 成员 h 之和,不去重),再计算该层;**下一层的读取可以和当前层的计算重叠**。读完最后一层、算完,全体成员同时得到首 token。

**共享存储与带宽 B**。所有 worker 的读取共用一条聚合带宽 B(GB/s),分配规则是 **FCFS**:按流的提交序号 `submit_seq` 排队,排在前面的先分满它的申请速率,剩余的给下一个。每个流有一个申请速率上限:

- 首层流直接申请满速 q_max;
- 下一层流按"刚好赶在当前层计算结束时读完"来申请(q = V/c);
- 到了预计读完的时刻还没读完(到期),申请速率提升到满速,但**排队序号不变**。

**STALL 与 COMPUTE**。worker 已领取 batch 但在等读取、没有在计算,记为 STALL;在计算记为 COMPUTE。STALL 高说明瓶颈在存储读取。

**batch 的收益与代价**。合批能摊薄启动开销、提高并行度(计算侧省时间);代价是**完整批屏障**:一 batch 内所有请求同时完成,短请求被长请求拖住。

**调度器与策略**。中央调度器看到的信息是受限的(只能看到已到达请求、worker 公开状态、存储的周期报价,看不到未来到达),它输出一组动作(哪个 worker 领哪个 batch,或者 WAIT 到某时刻)。比较的策略:

| 策略 | 规则 |
|---|---|
| **cq_fcfs**(基线) | 按到达顺序组 batch,空闲 worker 立即领取。对应 vLLM/SGLang 的默认调度机制在本架构下的投影 |
| cq_lpm | 命中前缀长的优先(对应 SGLang 可选的 longest-prefix-match) |
| **cq_spt** | T0 小的优先(T0 = 该请求单独执行时的参考服务时间;短任务优先) |
| cq_edf | deadline 早的优先 |
| cq_slack | 剩余富余时间(deadline − 当前时刻 − 预计服务时间)小的优先 |
| cq_pair | 按读/算互补关系组批 |
| cq_local | 与 MPC 同样候选,但评分时忽略 worker 之间的带宽竞争 |
| **cq_mpc** | 滚动预测:对候选动作做完整推演(用统一的 EDF 规则把所有已到达请求排空),按目标挑最优的第一个动作 |

**两个参照**。所有收益同时报两列:相对 **FCFS**,和相对 **B\***(训练集上冻结的最强简单基线——本次冒烟中就是 cq_spt)。

**控制开销**。调度器每次决策要花时间(构建候选、推演评分)。zero 模式假设不花时间;measured 模式把真实耗时计入服务轨迹。

---

## 3. 数据:三份 Mooncake trace

Mooncake(开源 KV cache 系统)公开发布了三份 trace,每行是 (timestamp 毫秒, input_length, output_length, hash_ids)。hash_ids 描述这条输入的 512-token 块序列,用来判断前缀命中。三份文件都做了 SHA256 校验:

| 文件 | 请求数 | 说明 |
|---|---:|---|
| conversation_trace | 12,031 | 对话流量 |
| toolagent_trace | 23,608 | 工具代理流量,前缀重复最多(约 40.8% 的 token 可命中) |
| synthetic_trace | 3,993 | **Mooncake 官方构造的合成 trace**,不是真实流量 |

处理方式:每份文件按时间切成三段——前 20% 的请求用来**建缓存目录**(frozen_history_catalog:它们的完整块预先驻留在共享存储,运行期间不变);中间 20% 是训练段;后 60% 是评价段。这样做保证所有策略面对**完全相同的命中机会**,不会因为某策略完成得快就获得更多缓存命中。

trace 里的时间戳按目标负载缩放(ρ = 负载 ÷ λ0,λ0 是该文件训练段算出的容量基准:conversation 6.68、toolagent 14.56、synthetic 8.66 请求/秒),token 数和命中关系不变。

---

## 4. 做了什么:模拟器,以及为什么不是真实系统

新增了独立的 `sim/cq/` 仿真器(12 个模块)和 6 个实验入口。**没有使用真实 GPU**:每个 batch 的每层计算时长由公式 synthetic-v1 给出(参数包括 N_sat=512、t_launch=50μs 等),读取按共享带宽 FCFS 分配精确推进。原因:① 公平——只让调度规则变,其他全部钉死;② 可复现——同种子同结果;③ 便宜。代价:所有绝对时间是合成机制结果,只能用于策略之间的**相对**比较,不构成硬件结论。

**正确性验证(四道保险)**:

1. **四计划金标(P1–P4)**:构造 4 请求、2 worker、2 层的极小场景,所有完成时刻手工用分数推演(例如 5021/576 秒),要求模拟器 Fraction 严格相等复现。四道全部通过,其中 P4 专门检验"同刻时旧 batch 的下层读取提交先于新 batch 的首次提交"这条规则——把顺序故意写反,P4 立即不通过。
2. **枚举/B&B 对拍**:对小场景求理论最优,穷举和带剪枝的分支定界两种独立实现,答案完全一致,且 B&B 给出下界=上界的证书。对拍抓出过一个真实的时刻顺序 bug。
3. **50 个新单元测试**:单位换算、FCFS 分配、到期提升、零读取/零计算边界、信息边界(策略拿不到未到达请求和存储真值)、读取字节守恒(两个策略总量相等)、右删失统计口径等,全部通过;旧系统 65 个测试零回归(共 115 passed)。
4. **trace 导入指纹**:三份文件用上述缓存目录规则推出的统计(建目录请求数 2121/4249/744、命中比例等)与规格预先手算的验收表逐位一致。

---

## 5. 六个实验各发现了什么

以下数字全部来自**冒烟级**运行(训练段时间块、决策零成本、4 策略子集)。正式评价(15 个评价块、8 策略、measured 成本)尚未运行,所以这些都是阶段性读数,不是最终结论。

### E19:trace 与计算画像的可信度

核对三份 trace 的完整性、命中分布、容量覆盖;验证 synthetic-v1 计算公式。结果:SHA256 全部匹配;trace-wide 能力组(262144 token / 128 GB)下 singleton 可行率 100%;公式抽查值与规格表逐位一致。没有实测硬件画像,`hardware_status=not_available`。

![E19 trace 概览](docs/figures/cq_fig_e19_trace_profile.png)

**看什么**:上排是三份 trace 的到达间隔分布(对数纵轴),下排是每请求 h(命中)与 u(新 token)的联合分布——三份负载形状差异很大,这是后续结论的前提。

![E19 组批计算曲线](docs/figures/cq_fig_e19_g_curve.png)

**看什么**:G_batch = 成员单独计算时间之和 ÷ batch 计算时间,>1 表示组批省计算。只有 u 小的请求(HS_US,一 batch 8 个)G≈4.07;u 大的(HL 类)G≈1——预告了 E21 的发现。

### E20:小场景的理论最优与在线策略差距

四道金标复现(全过);在 4 请求小场景上对 M(makespan)/S(失败数)/T(总 TTFT)/R(归一化 TTFT)四个目标分别求网格最优。n=4 的实例枚举与 B&B 一致、证书闭合;n=6 按规格保留 time_limit(超时不冒充通过)。

一个非直觉结果:网格最优 M=8.0,优于四个手算计划里最好的 P3(≈8.72)——**在领取时刻可以精确选择的设定下,合适的错峰比大小混批更好**。在线策略(不知道未来到达)在此场景离最优约 34%,这个差距就是"预知未来"的价值上限。

![E20 四计划金标](docs/figures/cq_fig_e20_four_plan.png)

**看什么**:四个计划的 M 与平均 TTFT(左)、STALL(右)。P4(错峰)平均 TTFT 最短、STALL 最少,但 M 最大——收益与取舍并存。

![E20 gap 与求解时间](docs/figures/cq_fig_e20_gap_runtime.png)

### E21:组批收益与同步损失

用真实 trace 的形状(每块前 32 条请求,arrival 全设 0)做有限工作集实验,对比三种能力档(A: n_max=1 禁止组批;B: 同 h-u 中位数象限才能合批;C: 自由)× 两种 profile(默认 / no_speedup)。

**核心发现:真实 trace 形状下 G_total = 1.00——batch 在计算侧完全不省时间。** 原因:真实请求的 u(几百到 125,683)远超 N_sat=512,η 恒为 1。对照:A 档(禁止组批、纯排序)下 SPT/EDF/MPC 的 SLO 满足率 0.961 vs FCFS 0.477——**排序本身的收益很大,而组批没有增量**。

![E21 组批权衡](docs/figures/cq_fig_e21_batch_tradeoff.png)

**看什么**:各策略的设备计算秒几乎相同(G=1 的体现),差异全在短请求 TTFT / SLO 上。

### E22:错峰的因果隔离

两层证据:(A) 确定算例——成员固定,只改 LL 的领取时刻(0 到 1 秒,带宽 2/4/8),记录完整的提交序号与每段速率;核对数值。(B) 真实 trace 固定批工作集——按 FCFS 预先分好 batch 目录,只比较领取顺序(immediate / order / wait),逐请求验证恒等式 **ΔTTFT = Δqueue + Δcompute + Δstall** 且固定成员的 **Δcompute = 0**(断言级验证,证明成员没被改动)。

结果:B=4 时领取延迟从 0 到 1.0,STALL 从 3.00 降到 2.875(−4.2%),M 从 11 升到 11.375(+3.4%)——**错峰降低等料,代价是总完成时间变长**,与解析示例一致。

![E22 时间线](docs/figures/cq_fig_e22_timeline.png)

**看什么**:右图每个横条是一个 worker 的时间线,蓝=COMPUTE、橙=STALL。错峰方案(P4)橙色总量少、整体更长。

![E22 等待分解](docs/figures/cq_fig_e22_wait_decomposition.png)

**看什么**:改动带来的 TTFT 变化拆成 Δqueue 和 Δstall 两部分——如果 STALL 降多少、中央排队就涨多少,说明等待只是换了地方,不是真收益。

### E23:主收益矩阵(冒烟)

2 文件 × B{20,80} × ρ{0.6,0.9} × θ{S,T} × 4 策略 × 2 训练块 = 128 runs,同 trace 配对比较。配对中位数:

| 策略 | SLO 满足率 (pp) | 平均 TTFT (%) | 相对 B\*(=spt) |
|---|---:|---:|---:|
| cq_spt | **+1.00** | **−14.40** | —(它就是 B\*) |
| cq_slack | +0.66 | −14.23 | ≈0 |
| cq_mpc | +0.78 | −14.26 | −0.03%(≈0) |

冒烟训练段上 B\* = SPT;**MPC 的全部收益与最强简单规则打平,没有额外增量**。两个负收益格点如实保留:synthetic B=80/ρ=0.9 下 FCFS 满足率(0.491)高于 SPT/MPC(0.478);E21 的 B 档下 MPC(0.242)低于 FCFS(0.469)。

![E23 收益总览](docs/figures/cq_fig_e23_gain_overview.png)

**看什么**:各策略相对 FCFS 的配对中位数收益,柱子在零线以上为正。这是训练段数据,正式结论须用评价段重跑。

![E23 负载曲线](docs/figures/cq_fig_e23_slo_load.png)

**看什么**:左图 FCFS 的 SLO 满足率随 ρ 上升而下降;conversation 在两种带宽下都已接近崩溃(读取带宽瓶颈),synthetic 有清晰坡度——实验区间选在 20%~95% 满足率之间,有区分度。

### E24:信息、观测与控制开销(单因素扫描)

固定一个重载格点(B=20, ρ=0.9, α=4, θ=S),每次只改一个因素。主要发现:

- 信息来源:σ=0 且 B 恒定时 history 与 current 等价(正确行为);
- **控制开销:MPC 的 SLO 收益从决策延迟约 2ms 开始被侵蚀(0.1667 → 0.1621),FCFS 不受影响**——给出了"何时退回简单规则"的第一条界线。

![E24 鲁棒性](docs/figures/cq_fig_e24_robustness.png)

**看什么**:四张小图分别是信息来源、采样周期、控制延迟、搜索深度对 SLO 满足率的影响。重点看第三张里 MPC 那条线在 2ms 后掉头。

---

## 6. 知道了什么、还不知道什么

**已确认(校准过的)**:模拟器、求解器、trace 导入的正确性都有硬证据;真实 trace 形状下组批无计算收益(G=1),调度价值在顺序与时机;好的简单排序(SPT)带来约 14% 的 TTFT 改善;MPC 未超过 B\*,且其收益对决策耗时敏感。

**未确认(下一步)**:正式 train(全 5 训练块冻结 B\*/gate)与 eval(15 评价块、8 策略、measured 成本两套)未运行;带宽噪声 σ、计算偏差 e、非平稳 B、协议对照等 E24 面板未实现;无实测硬件画像。**当前没有 go/no-go 判定,没有策略获胜的结论。**

---

## 附:术语速查表

| 术语 | 一句话解释 |
|---|---|
| TTFT | 请求到达到输出第一个 token 的时间 |
| SLO / deadline | 服务质量目标;本实验 deadline = arrival + α·T0,按时完成即满足 |
| T0 | 该请求单独执行(空系统、参考带宽)的服务时间,用于定 deadline 和排序 |
| h_tokens / u_tokens | 命中前缀 token 数(决定读取量)/ 新 token 数(决定计算量) |
| KV cache | 预计算好的中间结果,存远端共享存储,命中部分读取后不必重算 |
| worker | 推理实例,共 4 个,同时各执行一个 batch |
| B / q_max | 聚合存储带宽 / 单流申请速率上限(GB/s) |
| FCFS(存储) | 按 submit_seq 排队分配带宽:前面的先分满 |
| lookahead / 到期提升 | 下层流按"赶在计算结束时读完"申请低速;到期没读完提速但序号不变 |
| STALL / COMPUTE | worker 占用但等读取 / 正在计算 |
| batch / 完整批屏障 | 1~8 个请求合成一批;成员同时完成,短请求被拖住 |
| G_batch | 成员 singleton 计算时间之和 ÷ batch 计算时间,>1 表示组批省计算 |
| 组批 / 排序 / 错峰 | 调度的三个自由度:合哪些请求、先派谁、何时派 |
| FCFS(策略) | 到达序组批立即派发,vLLM/SGLang 默认机制的投影(主基线) |
| SPT / EDF / slack | 短任务优先 / 早截止优先 / 剩余富余时间小者优先 |
| MPC | 滚动预测调度:推演候选动作的完整排空结果再选首动作 |
| B\* | 训练集冻结的最强简单基线(本次冒烟中为 SPT) |
| λ0 / ρ | 该 trace 训练段算出的容量基准到达率 / 实际负载与 λ0 之比 |
| frozen_history_catalog | 用 trace 前 20% 请求建好的固定缓存目录,运行期不变,保证命中机会对等 |
| zero / fixed / measured | 控制开销模式:决策不耗时 / 固定延迟 / 实测耗时计入轨迹 |
| makespan (M) | 有限工作集全部请求完成时间 |
| 右删失 | 未完成请求只知 TTFT 下界(截断时刻 − arrival),仍计入分母 |
| 冒烟 (smoke) | 小规模验证运行;正式评价前所有实验数字均为冒烟读数 |
