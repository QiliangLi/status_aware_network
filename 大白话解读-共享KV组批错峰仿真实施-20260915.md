# 大白话解读:共享 KV 组批与错峰调度仿真,到底做了什么、发现了什么

> 这份文档**完全独立**,不需要配合任何其他文档阅读。所有概念都在文中从头讲起。
> 配套的技术文档是《仿真实验设计实施与测试规格》和《测试报告》,但你看不懂它们也没关系——本文就是给它们的"人话版"。

---

## 0. 三句话总结(先给结论)

1. 我们**造了一套经过严格校准的"模拟考场"**,用来公平比较不同的排队策略,而不是直接跑真实系统——校准方式是让模拟器复现四道"标准答案精确到小数点后无穷位"的手算题,全部逐位通过。
2. 考试用的是 **Mooncake 公开的三本真实订单记录**(里面有每次请求的到达时间、长度、和历史的重复关系),不是编造的数据。
3. 目前最重要的发现是一个"反直觉的坏消息":**真实订单里每个请求要现场处理的新内容太多,几单合在一起炒并不省工**——所以在这类负载下,调度器值得花力气抢的不是"合单",而是"先做谁、什么时候做"。

下面从头讲。

---

## 1. 这个研究在解决什么问题

你用大模型(比如问它一个问题)的时候,模型在**吐出第一个字之前**,要先把你整个问题读一遍、对每个字做一遍预计算。这段时间你看到的是"转圈圈"。从你按下回车到第一个字蹦出来,这段等待叫**首字时间**——它基本决定了"这个服务用起来卡不卡"。

一家大模型服务商每天要同时回答海量问题。机器(下文叫**后厨**)是有限的,于是每个请求都要排队。怎么排、怎么分配资源,直接决定用户等多久。这个研究就是在问:

> **同样的请求流、同样的机器、同样的数据仓库,只改变"排队的规矩",能让多少人按时吃上饭?**

---

## 2. 把整个系统翻译成一家餐厅

后文全部用这套类比,先建立一遍对照关系:

| 技术术语 | 餐厅里的说法 | 解释 |
|---|---|---|
| 请求(request) | 一位顾客点的单 | 要回答的一个问题 |
| 首字时间 TTFT | 顾客从下单到上第一口菜的等待 | 越短越好 |
| SLO / 截止时间 | 顾客能接受的最长等待 | 超过就算"差评"(失败) |
| **KV 缓存(旧料)** | 仓库里提前备好的半成品 | 之前有人点过几乎一样的菜,备好的部分不用重做 |
| 命中前缀 h | 能从仓库取的旧料份数 | 取料要走传送带,也要时间 |
| 新 token u | 必须现场处理的新鲜食材份数 | 这是后厨真正的炒菜工作量 |
| 推理实例(worker) | 灶台 | 有 4 个,一次各炒一锅 |
| **共享存储 + 带宽** | 一条所有灶台共用的传送带 | 从仓库往灶台送料,传送带是**全局唯一**的,先申请的先传、传满为止 |
| **合批(batch)** | 一锅同时炒好几道菜 | 灶台启动一次有固定开销,合炒能摊薄;但一锅里的菜要**同时出锅**,快菜会被慢菜拖住 |
| 调度器 | 传菜口的领班 | 决定:哪些单合锅、哪锅先下、要不要故意等等 |
| FCFS(基线) | 按排队先后接单 | 最常见的默认做法(vLLM / SGLang 的默认就是这个思路) |
| SPT | 先做简单的单 | 预计几分钟就能出菜的单优先 |
| EDF / Slack | 快到"差评线"的优先 | 谁再不做就要超时了就先做谁 |
| 错峰(stagger) | 让大单晚半分钟再取料 | 免得大单一上来就把传送带占死,把别人的小单都堵住 |

这套类比里有个关键设定,是本研究和常见仿真的最大区别:**旧料是共享的、传送带是唯一瓶颈**。也就是说,就算你灶台再空,料送不过来也只能干等(这种"灶台开着火却没料可炒"的状态,我们叫 **STALL**,等料时间)。而传送带的分配规矩是铁的"先来先服务":谁先申请,谁先全力传;先来的没传完,后来的就得排队。

领班手里有三个可以动的旋钮:

1. **合锅**(组批):哪些单凑一锅?
2. **顺序**(排序):空闲灶台先接哪单?
3. **时机**(错峰):要不要故意让某一锅晚一点下,给传送带腾地方?

转任何一个旋钮都可能有人受益、有人受损——比如等一等可能凑出更优的锅,但等的时候别的顾客在着急。这个研究就是要把这些取舍**量化**出来。

---

## 3. 为什么不直接跑真实系统,要造"模拟考场"

三个原因:

1. **公平**。真实系统里,你换个调度策略往往连带着改了一堆别的东西(缓存、引擎行为),赢了也说不清是赢在调度还是赢在别处。模拟器可以把"机器、传送带、订单、旧料仓库"全部钉死,**只让排队规矩变**,这才是干净的对照实验。
2. **可复现**。同样的题、同样的种子,跑一百遍结果一样,别人才能检查你。
3. **便宜且可控**。想试"传送带慢 4 倍""订单翻倍"这种场景,改个数字就行。

代价也说清楚:模拟器里"炒一道菜要几分钟"是用**数学公式**算的(不是在某块真实 GPU 上测的),所以所有绝对数字都是"合成机制结果",只能用来比较**策略之间的相对好坏**,不能当成真实硬件的性能指标。

那"考题"从哪来?用 Mooncake(月之暗面开源的 KV 缓存系统)公开发布的**三本真实订单记录**:

| 记录本 | 俗称 | 单量 | 特点 |
|---|---|---:|---|
| conversation | 对话记录 | 12,031 条 | 多轮聊天,和之前重复得多 |
| toolagent | 工具代理记录 | 23,608 条 | 重复得最多(约四成内容可复用旧料) |
| synthetic | **发布方自己造的**记录 | 3,993 条 | 注意:这本不是真实流量,是 Mooncake 官方合成的 |

每条记录里有什么:这单什么时候来(精确到毫秒)、问题有多长、以及"这单的开头和之前的哪些单相同"(用来判断能取多少旧料)。我们核对过三本的页数和"印章"(密码学哈希),与官方发布完全一致——**没读错题**。

旧料怎么算,有一条很重要的规矩:只有**从头开始连续相同**的部分才算旧料。打个比方:你点了"宫保鸡丁不要花生",之前有人点过"宫保鸡丁"——那"宫保鸡丁"这部分可以复用;但绝不能因为菜单里各处都出现过"鸡丁"两个字,就把不连续的部分也算进去。另外每单至少留一口现场做(哪怕整单都命中旧料),这是模型的执行方式决定的。

---

## 4. 怎么确认模拟器没算错(这是本次工作最硬的部分)

模拟器最怕的事:算错了但没人发现,后面所有结论全是空中楼阁。所以本次工作花了最大力气在校准上,用了四道保险:

**保险一:四道手算到无穷精度的考题(P1–P4)。**
我们构造了四个超小场景(4 位顾客、2 个灶台、每个只有 2 层工序),把所有事件时间**手工用分数推演**出来——比如某位顾客的上菜时刻精确等于 5021/576 秒这种级别的分数。然后让模拟器跑,要求它给出的答案和手算**逐位相等**(不是"误差小于多少",是完全相等)。四道题全部通过。

其中一道题还专门用来抓一种隐蔽的 bug:**同一时刻,旧锅取下一层料和新锅第一次下单,谁排前面?** 规矩是旧锅优先。我们故意把模拟器改成错误顺序,验证这道题立刻不通过——说明这道题真的在考这个规则,而不是碰巧蒙对。

**保险二:两种独立解法互相对拍。**
对于小场景,可以求出"理论最优排法"(上帝视角,穷举所有可能的下单时机)。我们写了两套求解程序:一套无脑穷举,一套聪明剪枝。在所有能穷举完的题上,**两套答案完全一致**,而且聪明那套能自己证明"我找到的就是最优"(给出下界=上界的证书)。这个对拍还真抓出来一个 bug——聪明剪枝版有个时刻顺序写错了,穷举版对出了差异,修掉之后两边完全一致。

**保险三:30+15 项单元测试。**
从"单位换算对不对"(每份食材到底多少克)到"传送带规矩对不对"(先来的占满、后来的等着、快超时的可以申请提速但排队位次不变),从"策略看不见不该看的信息"到"两个策略跑同一份订单、取走的料必须一字节不差地相等",共 50 个新测试全部通过,而且老系统的 65 个测试也没被改坏(共 115 个全绿)。

**保险四:题面指纹逐位核对。**
三本订单记录用我们的取旧料规则推出来的统计(多少单进仓库、多少单有旧料、平均几成内容可复用),和设计文档里预先手算的验收表**逐位一致**。

一句话:**这次交付的核心不是"某个策略赢了",而是"裁判和算盘先当众校准过了"。**

---

## 5. 考了哪些试,各发现了什么

六个实验(E19–E24),按"先验题面 → 验算盘 → 再做机制实验 → 最后打策略擂台"的顺序排。以下数字都来自**冒烟级**试跑(正式大考还没开考,见第 6 节)。

### E19:题面和工时表可信吗?

**做了什么**:核对三本订单记录的完整性;统计每本的到达节奏、单量分布、旧料比例;验证"炒菜工时公式"没问题。

**结论**:三本记录的"印章"全部对上;从训练段定出的系统容量基准分别是每秒 6.7 / 14.6 / 8.7 单;工时公式抽查值与设计表逐位一致。没有真实硬件的实测工时(所有工时是公式合成的),这一点在报告里明确标了"硬件不可用"。

![三本订单记录长什么样:到达间隔、每单旧料/新料分布](docs/figures/cq_fig_e19_trace_profile.png)

**这张图怎么看**:上排是三本记录的"下单间隔"分布(纵轴是对数,越靠右说明间隔越大的单越常见);下排是每单"旧料份数 vs 新料份数"的散点——可以直观看到三个来源的负载长相很不一样。

![工时公式的"合锅省钱"曲线](docs/figures/cq_fig_e19_g_curve.png)

**这张图怎么看**:横轴是一锅几单,纵轴是"合锅比分开炒省了几倍工"。只有"新料很少"的单(HS_US 那条线,一锅 8 单能省约 4 倍)合锅才明显省钱;新料多的单(HL 两条线)几乎贴着 1 不动——这直接预告了 E21 的发现。

### E20:算盘复验 + 小题上的"理论最优"

**做了什么**:四道手算考题机器复现(全过);在小场景上求四目标各自的理论最优;看在线策略(不知道未来订单)离理论最优差多少。

**结论**:小场景理论最优的"总完工时间"是 8.0 秒,而四道手算题里最好的混锅方案只有约 8.72——**在可以精确挑时机的世界里,"小单先走、大单紧跟"的错峰居然比"大小混一锅"更好**,这就是量化收益地图的价值:直觉会骗人。另外,在线策略因为看不见未来订单,在这道题上离理论最优有约三成差距——这三分之一的差距就是"预知未来"本身的价值上限,值得后续专门做对照实验。

![四道手算考题的复现结果](docs/figures/cq_fig_e20_four_plan.png)

**这张图怎么看**:左图是四种排法的"最后一单完成时间"和"平均等待",右图是各自的"灶台空等(STALL)"——可以看到 P4 方案(错峰那个)平均等待最短、空等最少,但最后一单完成得最晚。**没有全面赢家,只有取舍**。

![在线策略离理论最优的差距和求解耗时](docs/figures/cq_fig_e20_gap_runtime.png)

### E21:合锅到底省不省工?(本次最重要的发现)

**做了什么**:从三本记录里取真实订单的"形状"(每单的旧料/新料份数),做小规模批次实验,对比三种能力档位(禁止合锅 / 只许相似的单合锅 / 随便合锅)× 两种工时公式(正常 / 完全没有合锅加速)。

**结论**:**真实订单形状下,合锅在计算侧一分钱不省(G=1.00)**。原因是真实订单的新料份数太大(平均几千、最多 12.5 万份),远超"合锅加速"起作用的范围(合锅加速只对很短的单有效,见 E19 那张曲线图)。换句话说:

> **在这类真实负载上,"凑一锅一起炒"这条路基本堵死;领班真正值得花力气的是"先做谁"和"什么时候做"。**

对照:在"新料很少"的合成负载下,一锅 8 单确实能省约 4 倍计算——所以结论不是"合锅没用",而是"**合锅的收益取决于负载形状,真实对话负载吃不到这个收益**"。

![合锅权衡地图](docs/figures/cq_fig_e21_batch_tradeoff.png)

**这张图怎么看**:三张子图分别是各策略的"总炒菜时间"、"短单平均等待"、以及把两者画在一张图上的权衡地图(左下角是理想位置)。可以看到各策略的总炒菜时间几乎一样(G=1 的直接体现),差异全在等待时间上。

### E22:错峰到底有没有用?

**做了什么**:两层证据。第一层,手工构造的确定场景:同一批顾客不变,只把"大单取料时刻"从 0 改到 0.125/0.25/0.5/…秒,把传送带上每一段谁在传、传多快全部记下来核对;第二层,真实订单固定分锅后,只改"下锅顺序"和"是否等待",逐单核对"等待变化 = 队列变化 + 灶台空等变化"(数学恒等式,必须严格成立)。

**结论**:错峰是真实的取舍,不是免费午餐——让大单晚半分钟取料,**老顾客的菜少等了(空等时间最多降 4~20%),但最后一单总是更晚上桌(慢 2~3%)**。恒等式逐单验证通过,证明"固定顾客组合"的对比实验没有被偷换任何东西。

![错峰时间线](docs/figures/cq_fig_e22_timeline.png)

**这张图怎么看**:右图是两个方案的时间线,横条代表每个灶台的工作段——蓝色是"在炒",橙色是"开着火等料"。错峰方案(P4)橙色的总量明显更少,但整体拖得更长。

![等待搬家了吗](docs/figures/cq_fig_e22_wait_decomposition.png)

**这张图怎么看**:对比"改顺序/加等待"两种改动各自让"排队时间"和"灶台空等"变化了多少。如果空等降了、排队涨了同样的量,说明等待只是从灶台挪到了前台——这类"搬家式改善"是我们专门要识别出来、不许冒充收益的。

### E23:策略擂台(冒烟预选赛)

**做了什么**:两本记录 × 两种传送带速度 × 两种负载强度 × 两种目标 × 4 个策略 × 2 个时间窗 = 128 场对局,同题同料严格配对比较。

**结论**:

| 策略 | 相对"先来后到"的提升 | |
|---|---|---|
| 最短优先(SPT) | 平均等待 **−14.4%**,按时率 **+1.0 个百分点** | 最好的简单规则 |
| 快超时优先(Slack) | −14.2% / +0.7pp | |
| 复杂预测(MPC) | −14.3% / +0.8pp | **和最短优先基本打平** |

冒烟赛段上,**"最强简单规则"就是 SPT**,而会推演未来的复杂算法(MPC)没有比它多赚到钱。另外要如实报告两个**输的格点**:某个负载组合下先来后到反而比 SPT/MPC 好 1.3 个百分点;某个限制合锅的档位下 MPC 甚至比先来后到差——负收益格点全部保留在结果里,这是规矩。

![策略擂台总览](docs/figures/cq_fig_e23_gain_overview.png)

**这张图怎么看**:左图是 SLO 目标(按时率提升多少个百分点),右图是时延目标(平均等待降低百分之几)。柱子高于零线就是赢。注意这还只是预选赛(训练段数据)。

![负载-按时率曲线](docs/figures/cq_fig_e23_slo_load.png)

**这张图怎么看**:左图是"先来后到"在不同负载强度下的按时率——对话记录那两条线已经很贴地(接近崩溃),合成记录还有清晰的坡度,说明实验区间选得有效(既没有全场满分、也没有全场零分,才有区分度)。右图是各策略等待时间的散点分布。

### E24:"聪明"值多少钱?

**做了什么**:固定一个重载场景,每次只拧一个旋钮:给领班的信息新旧、信息更新频率、决策耗时长短、往前推演几步。

**结论**(最有操作价值的一条):**复杂算法的收益从"每次决策耗时约 2 毫秒"开始被吃掉**(按时率 16.7% → 16.2%),而简单策略完全不受影响。这划出了第一道"何时该退回简单规则"的界线——推理服务里首字本来就只要几十毫秒,2 毫秒的决策开销是真实存在的成本。

![鲁棒性扫描](docs/figures/cq_fig_e24_robustness.png)

**这张图怎么看**:四张小图分别是"信息来源 → 按时率""信息更新周期 → 按时率""决策延迟 → 按时率""推演深度 → 按时率"。重点看第三张:MPC 那条线在 2ms 之后开始掉头,FCFS 那条线纹丝不动。

---

## 6. 现在知道了什么、还不知道什么(诚实边界)

**已经知道的(校准过的)**:

- 模拟器、求解器、题面导入三者的正确性都有硬证据,可以放心当基础设施用;
- 真实对话类负载下,合锅不省计算,调度价值集中在**顺序**和**时机**;
- 好的简单排序(最短优先)能拿到约 14% 的平均等待改善;
- 复杂预测算法目前**没有**超过最好的简单规则,而且它的收益对决策耗时敏感。

**还不知道的(下一步)**:

- 以上擂台数字只是**预选赛**(训练段的两个时间窗、简化策略集合、决策零成本)。正式大考要用全部 15 个考试时间窗、8 个策略全员、计入真实决策耗时,再下"谁赢"的结论——目前**没有**赢家宣言;
- 真实硬件上的工时还没测(当前全是公式合成),所以一切数字只用于策略间相对比较;
- 带宽噪声、计算预估偏差、动态缓存等面板还没跑。

一句话收尾:**这次把考场建好、校准好、预选赛打完;决赛(正式评价)还没开打,而预选赛已经告诉我们——别指望合单,值得研究的是排队。**

---

## 附:和这两份技术文档的关系

- 《仿真实验设计实施与测试规格》= 这项研究的**法律文本**:每个概念、每条规则、每个验收数字的精确定义。本文第 2~4 节是它的白话版。
- 《测试报告》= 本次交付的**验收记录**:115 个测试的明细、四道手算题的完整数字、六个实验的数据表。本文第 5~6 节是它的白话版。
- 想看任何一条白话结论的精确出处,再去翻对应文档,不需要来回对照。
